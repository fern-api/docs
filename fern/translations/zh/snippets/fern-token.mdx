使用 `fern token` 生成一个用于在 CI/CD 环境中对 Fern CLI 进行身份验证的令牌。该令牌特定于您在 [`fern.config.json`](/learn/zh/sdks/overview/project-structure#fernconfigjson) 中定义的组织，并且不会过期。

<CodeBlock title="terminal">
```bash
fern token
```
</CodeBlock>