## 0.13.8
**`(fix):`** 修复线框测试请求类型命名，使用 IR 声明名称而不是基于端点的合成名称。这确保生成的测试使用正确的类型名称，如 ResponseChargeBack 而不是 AddResponseRequest。


## 0.13.7
**`(fix):`** 修复线框测试编译，避免在非 Default 类型上使用 Default::default()。在生成的线框测试中始终明确提供所有字段，而不是依赖 Default trait 派生。