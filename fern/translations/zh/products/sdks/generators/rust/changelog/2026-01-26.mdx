## 0.17.2
**`(fix):`** 为 base64 和 BigInt 类型添加 serde 辅助程序，以在 JSON 中正确序列化/反序列化这些类型。添加 execute_request_base64 方法来处理 base64 原始端点响应。修复联合变体 serde 重命名，使其对大小写敏感，以正确进行 JSON 反序列化。


## 0.17.1
**`(fix):`** 修复在生成没有流式端点的 SDK 时的编译错误，仅在存在流式端点时有条件地包含 sse_stream.rs。还修复了当 dateTimeType 设置为 "offset"（默认值）时 DateTime 类型缺失 FixedOffset 导入的问题。