## 0.16.0
**`(feat):`** 添加 `packageLicenseFile` 配置选项以支持自定义许可证文件。设置后，在 Cargo.toml 中使用 `license-file` 而不是 `license`，允许用户引用不是标准 SPDX 标识符的自定义 LICENSE.md 文件。


## 0.15.0
**`(feat):`** 向所有 HTTP 请求添加平台标头（X-Fern-Language、X-Fern-SDK-Name、X-Fern-SDK-Version）。这些标头有助于识别 SDK 使用情况和版本，用于 API 分析和调试。