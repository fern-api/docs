## 0.18.0
**`(feat):`** Swift SDK 现在通过 `enableWireTests` 选项（默认为 true）包含自动生成的线路测试。


## 0.17.5
**`(fix):`** 修复了一个错误，某些判别联合变体在其 `encode(to:)` 方法体中缺少容器声明语句。


## 0.17.4
**`(fix):`** 修复了 README 中 SPM 安装说明中缺少的 `gitUrl` 和 `minVersion` 值。


## 0.17.3
**`(fix):`** 将子客户端类初始化器访问级别降低为 `internal`。