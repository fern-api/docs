## 0.28.2
**`(fix):`** 在记录 README.md 和 reference.md 生成失败后重新抛出错误。
文档生成错误现在会中断生成流程，因此被视为重要的修复问题。


## 0.28.1
**`(fix):`** 修复动态片段以在生成的代码示例中包含全局头部。
之前，通过 `x-fern-global-headers` 配置的必需全局头部（例如 `X-Organization-ID`）
在动态片段输出中缺失。同时向 Swift 动态片段生成器添加了全局头部支持。