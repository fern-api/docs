## 3.42.1
**`(fix):`** 修复具有过多属性且超过 JVM 255 参数槽限制的对象的编译错误。像 CMS-1500 索赔表单这样的类型会导致 `too many parameters` javac 错误。生成器现在计算精确的 JVM 槽计数（`long`/`double` 消耗两个槽，所有其他类型消耗一个槽），当超过阈值时，生成一个接受 Builder 作为单个参数的构造函数，保持公共 API 不变。