## 4.1.1
**`(fix):`** 修复 `custom-dependencies` 无法覆盖捆绑依赖版本（OkHttp、Jackson 等）的问题。
以前，自定义依赖与捆绑依赖一起附加，因此 Gradle 的依赖解析会选择较高版本。
自定义依赖现在替换具有相同组和构件的任何捆绑依赖，允许用户固定特定版本以进行 CVE 修复或兼容性处理。


## 4.1.0
**`(feat):`** 添加 `maxRetries` 自定义配置选项，以覆盖失败请求的默认最大重试次数。
未指定时默认仍为 2。