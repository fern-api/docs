## 3.29.0
**`(feat):`** 为 OAuth 客户端凭据认证添加向后兼容的 `builder()` 方法。这恢复了对经典构建器模式 `Client.builder().token("...")` 和 `Client.builder().credentials("...", "...")` 的支持，同时保持现有的 `withToken()` 和 `withCredentials()` 快捷方式。这可以防止升级客户的破坏性更改。