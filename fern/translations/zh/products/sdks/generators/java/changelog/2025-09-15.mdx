## 3.4.0
**`(feat):`** 通过 `customSections` 配置选项添加对 README.md 中自定义部分的支持。


## 3.3.0
**`(feat):`** 线测试现在根据 API 的认证方案（Bearer、Basic、Header/Custom、OAuth 或 Inferred）自动配置认证，
而不是使用硬编码令牌。当可用时，从 IR 示例中提取真实认证值，使测试更准确和可维护。


## 3.2.2
**`(fix):`** 修复带有 Optional 字段的可空请求体，正确在 Optional 字段本身而不是整个请求对象上生成 .isPresent() 检查。


## 3.2.1
**`(fix):`** 将 Optional.of() 更改为 Optional.ofNullable() 以优雅处理 null 值，允许用户重置为默认版本。