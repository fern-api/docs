## 4.0.2
**`(fix):`** 修复包装请求端点（具有查询参数以及 `application/octet-stream` 主体的端点）的二进制文件上传。主体类型现在是 `byte[]` 而不是 `String`，请求通过 `InputStreamRequestBody` 发送而不是 `String.valueOf().getBytes(UTF_8)`，后者会损坏二进制数据。


## 4.0.1
**`(fix):`** 修复当启用 `collapse-optional-nullable` 时，生成的代码片段为仅可选（非可为空）字段产生 `OptionalNullable.of(...)` 的问题。