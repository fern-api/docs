## 3.44.2
**`(fix):`** 为未知类型别名生成命名的 Java 包装类。之前，当 API 模式没有 `type` 和 `properties`（表示"任何"类型）时，生成器删除命名类型定义并在引用处内联 `Object`。未知类型别名现在基于 `java.lang.Object` 生成适当的包装类（例如 `DocumentedUnknownType`），与其他别名类型的处理方式一致。