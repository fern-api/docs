## 0.9.8
**`(feat):`** `RequestOptions` 现在生成时 `timeout` 字段初始化为 `Optional.empty()` 而不是 `null`，以避免在构建器中未设置 `timeout` 时出现 NPE。

## 0.9.7
**`(feat):`** SDK 生成器现在在所有文件中生成 `@java.lang.Override` 而不是 `@Override`，以避免与可能在同一包中生成的任何 `Override.java` 类发生冲突。在此版本之前，大部分地方使用的是前者，但不是全部。