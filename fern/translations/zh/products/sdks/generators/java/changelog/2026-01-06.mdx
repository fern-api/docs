## 3.27.6
**`(feat):`** 当所有查询参数都是可选的时，生成不带请求包装器的重载方法。这可以防止向端点添加可选查询参数时出现编译中断。例如，现在除了请求包装器变体外，还会生成 `getLatestInsurance(String userId)` 和 `getLatestInsurance(String userId, RequestOptions requestOptions)` 重载方法。