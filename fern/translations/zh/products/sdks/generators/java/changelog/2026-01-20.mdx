## 3.33.0
**`(feat):`** 为具有必需请求体和可选头部/查询参数的端点生成仅请求体的方法重载。这可以防止在现有端点添加可选头部或查询参数时出现破坏性更改。


## 3.32.1
**`(fix):`** 更新容器以修复 CVE-2026-23745（node-tar 通过硬链接/符号链接的路径遍历）和 CVE-2025-66293（libpng 越界读取）。将 npm 绑定的 tar 升级到 7.5.3，并将 libpng16-16 更新到最新可用版本。