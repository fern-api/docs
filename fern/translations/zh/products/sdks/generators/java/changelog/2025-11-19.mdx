## 3.18.0
**`(feat):`** 添加自定义双向分页支持。使用自定义分页（例如 HATEOAS 链接）的 API 可以配置自定义分页器名称，并接收带有双向导航方法（`nextPage()`、`previousPage()`、`hasNext()`、`hasPrevious()`）的框架类。

配置：
```yaml
customConfig:
  custom-pager-name: MyCustomPaginator
```


## 3.17.1
**`(chore):`** 升级生成器 CLI 版本以发布新的 Docker 镜像。