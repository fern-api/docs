## 3.18.4
**`(fix):`** 修复某些 `OptionalNullable<>` 实例中缺失的 @JsonInclude 头部。


## 3.18.3
**`(fix):`** 修复启用 collapse-optional-nullable 时的 OptionalNullable 编译错误。更正类型见证语法以生成 `OptionalNullable.<Type>absent()`，并防止 OptionalNullable 类型的双重包装。


## 3.18.2
**`(fix):`** 修复 3.17.0 版本中添加的基于 InputStream 的方法重载中的错误。


## 3.18.1
**`(fix):`** 修复字节响应支持和代码片段生成。字节响应现在返回 `byte[]` 而不是抛出异常。代码片段中的空字节请求体现在生成有效的默认参数。