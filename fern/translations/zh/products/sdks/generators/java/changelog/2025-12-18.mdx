## 3.27.4
**`(fix):`** 修复 OAuth 客户端凭据流程，使用 ClientOptions.Builder.from() 模式在令牌请求中包含自定义请求头、超时和重试。


## 3.27.3
**`(fix):`** 修复流式端点返回类型使用 Iterable 而不是 Optional，修复空列表字面量缺少 ArrayList 导入，以及修复 OAuth wire 测试使用 withCredentials() 模式。


## 3.27.2
**`(chore):`** 支持通过 NODE_PATH 和 JAVA_V2_PATH 环境变量配置 Node.js 和 Java V2 可执行文件路径，用于本地种子测试。


## 3.27.1
**`(chore):`** 在生成的 metadata.json 文件中添加 sdkVersion 作为顶级字段。