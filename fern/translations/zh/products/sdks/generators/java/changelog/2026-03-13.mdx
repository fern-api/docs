## 3.43.0
**`(fix):`** 修复当服务器消息类型产生与生命周期处理程序相同的方法名时的 WebSocket 客户端编译错误。例如，消息类型 `error` 生成的 `onError(Consumer<Error>)` 由于 Java 泛型类型擦除而与生命周期 `onError(Consumer<Exception>)` 冲突。冲突的消息处理程序方法现在用 `Message` 后缀（例如 `onErrorMessage`）。