## 3.44.1
**`(fix):`** 修复在下载文件模式下 `enable-forward-compatible-enums` 配置标志被忽略的问题。该标志在 `JavaSdkDownloadFilesCustomConfig` 中缺失，因此在使用 `--local` 生成时，generators.yml 中的 `enable-forward-compatible-enums: false` 没有效果。