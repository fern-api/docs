## 3.42.8
**`(fix):`** 为基于接口的区分联合包装类添加 `equals()`、`hashCode()` 和 `toString()` 方法。之前，外部包装类回退到 `Object.equals()`（引用标识），破坏了任何传递性包含联合的类型的相等性。这些方法现在委托给内部 `Value` 字段。


## 3.42.7
**`(chore):`** 为生成的 CI 工作流添加并发配置，设置 cancel-in-progress: false，以防止当新运行开始时取消堆叠的运行。