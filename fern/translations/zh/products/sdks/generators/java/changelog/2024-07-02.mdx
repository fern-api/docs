## 1.0.2-rc0
**`(feat):`** 生成器现在为每个生成的类型添加类级别的 `@JsonInclude(JsonInclude.Include.NON_ABSENT)` 注解，以替代之前默认的 `@JsonInclude(JsonInclude.Include.NON_EMPTY)`。这可以在 `generators.yml` 文件中配置：
```yaml
generators:
  - name: fernapi/fern-java-sdk
    config:
      json-include: non-empty # 默认为 non-absent
```