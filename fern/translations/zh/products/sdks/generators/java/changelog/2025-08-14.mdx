## 2.41.0
**`(feat):`** 通过 `enable-wire-tests` 标志添加生成线测试的支持。线测试使用 MockWebServer 验证 HTTP 协议通信，并为服务中的所有端点生成。

```yaml
customConfig:
  enable-wire-tests: true
```

生成的测试包括：
- 可立即编译的有效 404/500 错误测试
- 带有 TODO 注释的成功测试模板，可供自定义
- 对分阶段构建器与常规构建器的正确处理
- 为所有魔术字符串提供常量，以提高可维护性

注意：复杂的请求体和响应验证需要手动自定义。