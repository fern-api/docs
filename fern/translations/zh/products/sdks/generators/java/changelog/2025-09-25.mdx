## 3.6.2
**`(fix):`** 修复了 SDK 变量设置器错误引用不存在的 `clientOptionsBuilder`。
变量现在存储在实例字段中，并通过 `setVariables()` 方法应用。


## 3.6.1
**`(fix):`** 修复了 wire 测试因 `Optional<T>` 序列化的 `InvalidDefinitionException` 而失败，
改为使用配置的 `ObjectMappers.JSON_MAPPER` 而不是普通的 `ObjectMapper`。


## 3.6.0
**`(feat):`** 添加对自托管/本地 SDK 生成的 PR 模式支持。


## 3.5.10
**`(fix):`** 代码片段现在在构建器中显示客户端级变量，而不是方法参数。