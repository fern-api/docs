## 3.9.2
**`(fix):`** 修复带包装请求体的 PATCH 请求中可空字段序列化问题。显式 null 值
(Nullable.ofNull()) 现在正确序列化为 `{"field": null}`，而不是像 Nullable.empty() 那样被省略。


## 3.9.1
**`(fix):`** 修复原始客户端方法在使用响应属性 (x-fern-sdk-return-value) 处理区分或无区分联合时
返回联合类型而不是第一个变体。