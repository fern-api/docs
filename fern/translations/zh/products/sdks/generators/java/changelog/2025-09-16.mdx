## 3.5.0
**`(feat):`** 增强的线测试框架，具有全面的 SDK 验证功能。线测试现在包括来自 IR 示例的头部验证、
多 URL 环境配置支持、对联合/可空/泛型类型的复杂类型验证，以及对 `Iterable<T>` 支持的完整分页测试。


## 3.4.1
**`(fix):`** 修复当未区分的联合包含解析为相同 Java 类型的多个成员（例如，多个字符串、字符串与字符串枚举）时的编译错误。
生成器现在通过在需要时为工厂和访问者方法添加数字后缀（of2、of3、visit2、visit3）来正确区分重复的方法签名。