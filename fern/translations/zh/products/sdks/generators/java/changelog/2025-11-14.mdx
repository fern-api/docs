## 3.16.0
**`(feat):`** 添加可选的 use-local-date-for-dates 配置标志，为 Fern 日期类型生成 java.time.LocalDate 而不是 String。


## 3.15.1
**`(fix):`** 修复具有自定义分页的端点的 SDK 生成失败。使用 `x-fern-pagination: type: custom` 的端点现在成功生成，返回原始响应类型而不使用自动分页包装器。