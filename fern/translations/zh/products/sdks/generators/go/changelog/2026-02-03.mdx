## 1.23.6
**`(fix):`** 修复多部分文件上传以支持内存读取器（bytes.Reader、strings.Reader 等），通过确保 Content-Disposition 标头中始终存在 filename 属性。当未提供文件名时，将使用字段名作为默认文件名。

## 1.23.5
**`(fix):`** 修复 wire 测试以使用配置的 `clientConstructorName` 而不是硬编码的 `NewClient`。