## 1.34.0-rc.0
**`(feat):`** 升级到 IR v66，压缩了 IR Name 类型，减少了 IR 大小并提高了性能。


## 1.32.2
**`(fix):`** 修复当 module.path 已包含 /vN 后缀且配置了 packagePath 时，导入路径中出现重复主版本号后缀的问题。现在 /vN 后缀在追加 packagePath 之前应用到模块路径，防止出现类似 github.com/owner/repo/v2/pkg/v2 的路径。