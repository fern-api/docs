## 1.12.0
**`(feat):`** 通过 `customSections` 配置选项在 README.md 中添加自定义部分的支持。


## 1.11.3
**`(chore):`** 更新重试器以与 TS SDK 的策略完全匹配；特别是，
在 `retry-after` 上不使用抖动，而是完全遵循它。


## 1.11.2
**`(fix):`** 修复索引 >= 64 的属性的位字段初始化。