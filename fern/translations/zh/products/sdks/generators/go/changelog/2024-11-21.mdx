## 0.33.0
**`(feat):`** 添加对 `inlinePathParameters` 配置选项的支持，在生成的请求类型中生成路径参数（如果有的话），而不是作为单独的位置参数。
```yaml # generators.yml
- name: fern-api/fern-go-sdk
  version: 0.33.0
  config:
    inlinePathParameters: true
```