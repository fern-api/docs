## 0.10.0
**`(feat):`** 将 `ClientOption` 重构为 `RequestOption`。

**`(feat):`** 添加 `includeLegacyClientOptions` 生成器配置。

**`(feat):`** 支持幂等性请求头作为仅在幂等端点上可用的特殊 `RequestOption`。

**`(fix):`** 路径参数文档的位置。

**`(fix):`** 定义多个字面量的无区分联合的命名冲突问题。