## 1.21.0
**`(feat):`** 通过 `x-fern-server-name` 扩展添加对多 URL 环境的支持。每个环境具有多个基础 URL 的 API 现在可以自动将不同的端点路由到不同的基础 URL。SDK 生成一个命名的 `Environment` 结构体，为每个基础 URL 提供字段，端点使用 `ResolveEnvironmentBaseURL` 函数解析其基础 URL。