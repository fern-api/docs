## 1.0.0
**`(feat):`** 主要版本发布，更新了生成器的默认配置选项集。
以下配置选项已更新：
  - `alwaysSendRequiredProperties` 现在默认为 `true`。
  - `inlineFileProperties` 现在默认为 `true`。
  - `inlinePathParameters` 现在默认为 `true`。
  - `useReaderForBytesRequest` 现在默认为 `true`。
  - `union` 现在默认为 `v1`。