## 1.21.8
**`(fix):`** 使用 X-Test-Id 头将请求范围添加到线路测试中。每个测试现在发送一个唯一的头，用于过滤 WireMock 请求验证，实现安全的并发测试执行，不会在测试之间产生干扰。

## 1.21.7
**`(chore):`** 修复调用者中的空值检查

## 1.21.6
**`(chore):`** 添加 GO_V2_PATH 环境变量支持，在本地开发和测试期间支持可配置的 v2 二进制路径

## 1.21.5
**`(fix):`** 修复线路测试中重复测试函数生成。当多个端点映射到同一测试函数名时，后续出现的函数现在获得数字后缀（例如 Test2、Test3）以避免 Go 编译错误，同时保留所有测试覆盖。

## 1.21.4
**`(chore):`** 在生成的 metadata.json 文件中添加 sdkVersion 作为顶级字段。