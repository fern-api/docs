## 1.23.4
**`(fix):`** 修复 cycle.go 中的不安全指针解引用问题。

## 1.23.3
**`(fix):`** 修复详尽夹具的 wire 测试生成。包含以下几个修复：
- 通过显式转换为 int64 来修复 long 类型的 int vs int64 类型不匹配问题
- 修复枚举值以对指针类型使用 .Ptr() 方法
- 修复字段名以使用 Go 导出名称（例如使用 FieldString 而不是 String）
- 为 []byte 指针类型添加 Bytes 辅助函数
- 在 wire 测试中跳过返回原始日期类型的端点（Go SDK 日期解析问题的解决方案，time.Time 期望 RFC3339 日期时间格式）