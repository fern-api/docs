## 1.22.1
**`(fix):`** 在 metadata.json 中为 sdkVersion 添加 'v' 前缀以遵循 Go 模块版本约定。

## 1.22.0
**`(feat):`** 为多命名空间 Go SDK 按命名空间生成 error_codes.go。每个命名空间现在都有自己的 error_codes.go 文件，仅包含该命名空间的错误代码和错误类型，而不是包含所有错误的单个全局文件。