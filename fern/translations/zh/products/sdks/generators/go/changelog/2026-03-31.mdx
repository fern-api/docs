## 1.32.1
**`(fix):`** 在保留字中包含 internal，以避免尝试导出名为 internal 的顶级包，这违反了 go 的规则。


## 1.32.0-rc1
**`(fix):`** 修复当判别器键存在于嵌套对象中时 SSE 事件判别器注入跳过的问题。以前，`injectDiscriminator` 使用简单的子字符串检查，在任何嵌套深度匹配键，导致像 `{"event":{"type":"user.created"}}` 这样的负载反序列化失败，其中 `"type"` 只出现在嵌套中。检查现在只考虑顶级 JSON 键。