## 3.30.0
**`(feat):`** 新增 `offsetSemantics` 标志，它改变了 `x-fern-pagination` 中 `step` 属性的解释方式。
当 `offsetSemantics` 为 `item-index`（默认值）时，偏移量被理解为项目索引，按获取的实体数量递增。
当 `offsetSemantics` 为 `page-index` 时，偏移量被理解为页面索引，每次递增 1。