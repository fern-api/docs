## 3.53.4
**`(fix):`** 修复联合成员接口遮蔽内置 TypeScript 类型如 `Date` 和 `Error` 的问题。联合变体如 `date` 在联合命名空间内生成 `export interface Date`，这会遮蔽全局 `Date` 类型并在其他变体引用它时导致编译错误。接口名称现在使用 `safeName`（例如 `Date_`、`Error_`）以避免与保留标识符冲突。