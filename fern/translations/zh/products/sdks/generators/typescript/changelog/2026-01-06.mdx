## 3.43.4
**`(fix):`** 修复从请求类型导出内联枚举常量值的问题。之前，具有内联枚举（直接在 OpenAPI 模式中定义）的请求类型使用 `export type { ... }` 导出，这只导出类型，而不导出运行时常量值。SDK 使用者无法在运行时访问像 `ConvertRequest.Resource.Auto` 这样的枚举值。现在请求类型使用 `export { ... }` 导出，同时导出类型和常量。