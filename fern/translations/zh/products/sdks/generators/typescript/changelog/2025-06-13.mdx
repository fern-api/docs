## 1.6.0
**`(feat):`** 您现在可以指定是否使用流包装器返回流，或返回 Web 标准流。
通过在生成器配置的 `config` 中将 `streamType` 设置为 `wrapper` 或 `web` 来更改返回的流类型。
默认值为 `wrapper`。


**`(internal):`** `tests/unit/zurg` 移动到 `tests/unit/schemas` 以匹配 `src/core/schemas` 中的名称，这是测试正在验证的内容。