## 3.46.3
**`(chore):`** 将 oxfmt 更新到 0.27.0，oxlint 更新到 1.42.0，oxlint-tsgolint 更新到 0.11.4。


## 3.46.2
**`(fix):`** 修复当启用访问者工具时生成枚举出现的 TypeScript 循环引用错误 (TS7022/TS2456)。
枚举类型别名现在引用一个中间的 `Values` 常量，而不是使用 `Omit<typeof EnumName, "_visit">`，
这避免了类型自身初始化器中的循环引用。