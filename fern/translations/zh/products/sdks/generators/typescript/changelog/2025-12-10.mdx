## 3.39.2
**`(fix):`** 修复具有多个根级端点的 API 的 reference.md 生成。
以前，只有最后一个根端点出现在参考文档中，因为 `addRootSection()` 在端点循环内被调用，每次都替换根部分。
修复将部分创建移到端点循环之前，确保包含所有根端点，并删除生成的空 `##` 部分标头。


## 3.39.1
**`(fix):`** 修复分页端点的 wire 测试，当 `response.data` 不存在时期望空数组 `[]` 而不是 `undefined`。
这与分页器的行为一致，当 data 属性为 undefined 时使用 `?? []` 返回空数组。