## 0.39.3
**`(fix):`** 多部分表单数据单元测试仅在 SDK 具有多部分表单上传时才会生成。

## 0.39.2
**`(fix):`** 允许在 Node 18+ 和浏览器中从底层 File 对象传递文件名
用户现在可以这样提供文件，使用简单的多部分上传 API 作为示例：
```typescript
client.file.upload(new File([...blobParts], 'filename.ext'), ...)
```
`filename.ext` 将被编码到上传中。