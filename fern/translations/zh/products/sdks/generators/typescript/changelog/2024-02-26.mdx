## 0.12.0
**`(feat):`** 支持深度对象查询参数序列化。如果查询参数是对象，那么 Fern 将支持序列化它们。

```yaml
MyFoo:
  properties:
    bar: optional<string>

query-parameters:
  foo: MyFoo
```

现在将被序列化为 `?foo[bar]="...` 并在 SDK 中显示为常规对象：

```ts
client.doThing({
  foo: {
    bar: "..."
  }
});
```