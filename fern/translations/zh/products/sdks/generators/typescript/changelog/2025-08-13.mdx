## 2.8.0
**`(feat):`** 选择使用 `pnpm` 或 `yarn` 作为生成的 SDK 的包管理器。
在 _generators.yml_ 中这样配置：
```yml
# In generators.yml
groups:
  ts-sdk:
    generators:
      - name: fernapi/fern-typescript-sdk
        config:
          packageManager: pnpm
```
默认是 `yarn`。