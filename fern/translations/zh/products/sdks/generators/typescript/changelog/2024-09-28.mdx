## 0.40.8
**`(fix):`** 文件数组上传现在调用 `request.appendFile` 而不是 `request.append`，后者会导致表单数据处于损坏状态。

## 0.40.7
**`(fix):`** 生成的 README 现在将包含一个链接到生成的 SDK 参考文档（在 `reference.md` 中）的部分。

```md
## Reference

A full reference for this library can be found [here](./reference.md).
```