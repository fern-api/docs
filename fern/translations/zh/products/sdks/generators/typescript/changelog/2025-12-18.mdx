## 3.42.8
**`(fix):`** 修复分页端点的 wire 测试生成。测试生成器现在在检查分页结果的 JSON 示例时正确使用 wire 格式（snake_case），并通过跳过 `hasNextPage()` 断言来正确处理具有 null 或缺失游标字段的示例。


## 3.42.7
**`(chore):`** 在生成的 metadata.json 文件中将 sdkVersion 添加为顶级字段。