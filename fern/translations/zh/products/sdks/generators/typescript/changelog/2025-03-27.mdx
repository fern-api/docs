## 0.49.7
**`(fix):`** 当 `useLegacyExports` 配置为 `false` 时，显著提高 SDK 生成的性能。对于像 Square 这样的大型规范，生成时间从 10+ 分钟缩短到几乎 1 分钟。

## 0.49.6
**`(feat):`** 在连接握手期间支持任意 WebSocket 头。

## 0.49.5
**`(feat):`** 改进 WebSocket 代码生成质量。