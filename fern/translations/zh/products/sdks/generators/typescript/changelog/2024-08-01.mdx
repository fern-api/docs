## 0.38.2
**`(fix):`** 重构了 `noScripts` 功能标志，确保不会意外触发任何 `yarn install` 命令。

## 0.38.1
**`(feat):`** 引入了名为 `noScripts` 的功能标志，防止生成器运行任何脚本，如 `yarn format` 或 `yarn install`。如果任何脚本导致错误，启用此选项将允许您接收生成的代码。

```
- name: fernapi/fern-typescript-sdk
  version: 0.38.1
  config:
    noScripts: true
```