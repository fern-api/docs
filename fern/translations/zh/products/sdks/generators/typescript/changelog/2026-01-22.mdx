## 3.44.1
**`(fix):`** 修复具有 date/datetime 类型的可选查询参数在为 undefined 时抛出序列化错误的问题。之前，作为 Date 别名的命名类型的可选查询参数（如 `before?: Before`，其中 `Before = Date`）会无条件调用 `jsonOrThrow`，在省略参数时导致"Expected Date object. Received undefined"错误。现在生成器为可选的 date/datetime 查询参数添加 null 检查，匹配已用于对象类型查询参数的行为。