## 0.51.0
**`(feat):`** 在 JavaScript 错误中添加 `rawResponse` 属性。

```ts
try {
  const fooBar = await client.foo.bar("id", options);
} catch (e) {
  if (error instanceof FooError) {
    console.log(error.rawResponse);
  } else {
    // ...
  }
}
```