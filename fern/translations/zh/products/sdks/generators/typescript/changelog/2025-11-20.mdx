## 3.35.0
**`(fix):`** 修复 validateAndTransformExtendedObject 以添加 null/undefined 保护。


## 3.33.0
**`(feat):`** 为基础认证实现认证提供者架构，与推断认证建立的模式保持一致。基础认证现在使用专用的 `BasicAuthProvider` 类实现 `AuthProvider` 接口，替换内联认证逻辑。


## 3.32.1
**`(fix):`** 为生成的发布工作流添加 contents:read 权限，用于 OIDC 认证以修复 actions/checkout@v4 要求。