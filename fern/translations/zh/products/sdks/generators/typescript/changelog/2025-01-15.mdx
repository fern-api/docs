## 0.47.1
**`(fix):`** 解决了可为 null 的查询参数在其方法调用中不是 null 安全的问题。生成的代码现在适当地防护 `null` 值，如下所示：

```typescript
const _queryParams: Record< ... >;
if (value !== undefined) {
    _queryParams["value"] = value?.toString() ?? null;
}
```