## 0.42.7
**`(feat):`** 支持 OpenAPI 中的 `additionalProperties` 或 Fern 定义中的 `extra-properties`。现在，标记为 true 的 additionalProperties 对象将生成以下接口：

```ts
interface User {
  propertyOne: string;
  [key: string]: any;
}
```