## 0.48.7
**`(fix):`** 表单数据编码现在正确处理数组和对象值，通过将每个属性值编码为单独的键值对，而不是尝试将整个对象编码为单个值。这确保了多部分表单请求中复杂数据结构的正确处理。

## 0.48.6
**`(fix):`** 通过使用 `qs` 以 `repeat` 数组格式正确编码数组和对象值，支持表单编码的表单数据参数。

## 0.48.5
**`(fix):`** 如果用户将 blob 上传到多部分表单，则不要双重包装 blob。否则文件的 content-type 在 Deno 中会丢失。