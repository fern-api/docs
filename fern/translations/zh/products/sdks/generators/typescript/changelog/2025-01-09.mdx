## 0.46.7
**`(fix):`** 简化运行时检测以减少使用不支持的 API（如 `process.`）的机会。检测 Vercel 的 Edge Runtime。

## 0.46.6
**`(fix):`** 将 `@types/node` 更新到 `18+`，这是生成的 `Node18UniversalStreamWrapper` 测试所需的。

## 0.46.5
**`(fix):`** 修复 webpack 测试以在 TypeScript 中使用 .js/.jsx 扩展名

**`(fix):`** 仅在 Jest 中映射 .js 模块，而不是 .json 文件。

## 0.46.4
**`(fix):`** 修复 packageJson 自定义配置和 package.json 类型字段。

## 0.46.3
**`(fix):`** 默认恢复使用遗留导出。

## 0.46.2
**`(fix):`** 修复 Jest 以处理使用 `.js` 扩展名导入的文件。

**`(fix):`** 确保 Jest 无论 package.json 类型如何都加载 Jest 配置。