## 3.53.11
**`(fix):`** 在记录 README.md、reference.md 和 CONTRIBUTING.md 生成失败后重新抛出错误。文档生成错误现在会中断生成流程，因此被视为需要修复的重要问题。

## 3.53.10
**`(fix):`** 修复动态代码片段以在生成的代码示例中包含全局头部。之前，通过 `x-fern-global-headers` 配置的必需全局头部（例如 `X-Organization-ID`）在动态代码片段输出中缺失。还修复了头部值查找使用有线值而不是不存在的属性。