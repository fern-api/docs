## 3.9.3
**`(fix):`** 修复路径参数可为空或可选时的编译错误。


## 3.9.2
**`(fix):`** 匹配 wire 测试生成中的逻辑，在静态示例生成中，当可选时对空对象省略请求参数。
这修复了一些由于请求体不匹配而失败的 wire 测试。


**`(fix):`** 在启用 `useBigInt` 时，向 `toJson` 传入 `undefined` 时返回 `undefined`。