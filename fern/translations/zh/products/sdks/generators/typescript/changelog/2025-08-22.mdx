## 2.9.1
**`(fix):`** 引入一个名为 `flattenRequestParameters` 的自定义配置，将引用的主体折叠到请求中，而不是嵌套在主体键下。

**之前**：
```ts
client.users.create({
  userId: "...",
  body: {
    "name": "Joe Scott"
  }
});
```

**之后**
```ts
client.users.create({
  userId: "...",
  "name": "Joe Scott"
});
```