## 3.5.0
**`(feat):`** 在 _tsconfig.esm.json_ 中启用 `verbatimModuleSyntax` 以提高 ESM 构建（非 CJS）的 TypeScript 性能。
您可能需要更新自定义 TypeScript 代码以符合此设置。


## 3.4.0
**`(feat):`** 添加 `wireTestsFallbackToAutoGeneratedErrorExamples` 配置选项，用于控制在用户未提供错误示例生成 wire 测试时是否使用自动生成的错误示例。