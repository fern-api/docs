## 2.12.2
**`(fix):`** 为内联类型的分页生成正确的类型。

## 2.12.1
**`(fix):`** 如果属性是可选的或可为空的，则为身份验证和分页生成 `?.` 属性访问器；如果属性是必需且不可为空的，则生成 `.` 属性访问器。


## 2.12.0
**`(feat):`** 通过 `customSections` 配置选项在 README.md 中添加自定义部分支持。


## 2.11.2
**`(fix):`** 当没有查询参数且身份验证方案具有自定义身份验证标头时，WebSocket 客户端生成可以正常编译。