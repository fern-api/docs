## 3.17.0
**`(feat):`** 在顶级 `exports.ts` 文件中添加 `Page`，这允许 `import { Page } from '...';` 正常工作。
移除 `Pageable`，在所有分页中仅使用 `core.Page`。


## 3.16.0
**`(feat):`** 生成 CONTRIBUTING.md 文件。


## 3.15.0
**`(feat):`** 为 `ReconnectingWebSocket`、`ReconnectingWebSocket.Event`、`ReconnectingWebSocket.CloseEvent` 和 `ReconnectingWebSocket.ErrorEvent` 导出类型。


## 3.14.0
**`(feat):`** 在 `Page` 类型上公开底层响应。