## 3.73.0
**`(feat):`** 在 TypeScript SDK Stream 类中添加对 SSE `id` 和 `retry` 字段的支持。
新的 `withMetadata()` 方法返回一个 `AsyncIterable<ServerSentEvent<T>>`，
在解析的 `data` 之外暴露每个事件的 `id`、`retry` 和 `event` 字段。
默认迭代仍然产生 `T`，因此完全向后兼容。
