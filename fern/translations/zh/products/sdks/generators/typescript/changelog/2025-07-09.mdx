## 2.4.6
**`(fix):`** 如果响应的 content-type 头是 JSON，则将 HTTP 错误体解析为 JSON，否则回退到文本。


**`(fix):`** 修复 `bytes` fetcher 测试。


**`(fix):`** 修复在 _generators.yml_ 配置中指定 `packagePath` 时的 Jest 配置。


## 2.4.5
**`(fix):`** 确保 `extraDependencies`、`extraPeerDependencies`、`extraPeerDependenciesMeta` 和 `extraDevDependencies` 总是合并到 _package.json_ 中。


## 2.4.4
**`(fix):`** 使 `BinaryResponse.bytes` 函数为可选，因为某些版本的运行时在 fetch `Response` 上不支持该函数。