## 0.51.3
**`(fix):`** 修复在 Node 16 及以下版本中 polyfill Headers 的轻微类型问题。

## 0.51.2
**`(fix):`** 上传文件时，如果给定对象上存在 `path` 属性，则从中提取文件名。
例如，这将为 `fs.createReadStream()` 提取文件名。

## 0.51.1
**`(fix):`** 如果原生 `Headers` 类不可用，则回退到自定义 `Headers` 类实现。
Node 16 及以下版本不支持原生 `Headers` 类，因此此回退是确保兼容性所必需的。