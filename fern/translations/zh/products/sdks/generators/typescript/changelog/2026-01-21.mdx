## 3.44.0
**`(feat):`** 当启用 serde 层时启用前向兼容枚举。之前，`enableForwardCompatibleEnums` 选项仅在 `noSerdeLayer` 为 true 时工作。现在当启用 `enableForwardCompatibleEnums` 时，枚举类型无论 serde 层设置如何都包含 `| string`，允许 TypeScript SDK 接受来自 API 的未识别枚举值。