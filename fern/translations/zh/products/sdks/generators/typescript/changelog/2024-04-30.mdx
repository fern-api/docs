## 0.16.0-rc7
**`(feat):`** SDK 生成器现在支持端点参数的 `@param` JSDoc 注释。生成器现在将 JSDoc 安排在几个独立的组中，分别针对 `@param`、`@throws` 和 `@examples`，如下所示：

```ts
  /**
  * This endpoint checks the health of a resource.
  *
  * @param {string} id - A unique identifier.
  * @param {Service.RequestOptions} requestOptions - Request-specific configuration.
  *
  * @throws {@link Acme.UnauthorizedRequest}
  * @throws {@link Acme.BadRequest}
  *
  * @example
  *     await testSdk.health.service.check("id-2sdx82h")
  */
  public async check(id: string, requestOptions?: Service.RequestOptions): Promise<void> {
    ...
  }
```

**`(feat):`** 生成器只有在用户提供的示例存在时才会包含它们，否则只包含一个生成的示例，如下所示：

```ts
  /**
  * This endpoint checks the health of a resource.
  *
  * @example
  *     await testSdk.health.service.check("id-2sdx82h")
  */
  public async check(id: string, requestOptions?: Service.RequestOptions): Promise<void> {
    ...
  }
```

**`(fix):`** SDK 生成器现在会转义之前会创建无效 URL 的路径参数（例如 "\\example"）。方法实现现在会有对 `encodeURIComponent` 的引用，如下所示：

```ts
const _response = await core.fetcher({
  url: urlJoin(
    (await core.Supplier.get(this._options.environment)) ?? environments.AcmeEnvironment.Prod,
    `/users/${encodeURIComponent(userId)}`
  ),
  ...
});
```

## 0.16.0-rc6
**`(fix):`** 代码片段模板现在将文件上传参数移动到未命名的参数中

## 0.16.0-rc5
**`(fix):`** 删除代码片段模板中的重复引号