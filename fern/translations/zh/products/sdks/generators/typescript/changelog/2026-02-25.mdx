## 3.51.1
**`(chore):`** 将 Docker 基础镜像从 Node.js 22 升级到 Node.js 24 LTS。


## 3.50.1
**`(fix):`** 通过 `connectMethodName` IR 字段（来源于 AsyncAPI 通道上的 `x-fern-sdk-method-name`）支持自定义 WebSocket 连接方法名称。指定时，生成的客户端使用自定义名称而不是默认的 `connect`。