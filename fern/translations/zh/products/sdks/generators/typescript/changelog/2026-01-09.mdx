## 3.43.8
**`(fix):`** 修复 wire 测试模拟服务器以忽略分页测试中的游标不匹配。当分页测试调用 `getNextPage()` 时，SDK 正确地从响应中发送游标，但模拟服务器因为游标与原始请求不匹配而拒绝请求。模拟服务器现在接受 `ignoredFields` 选项来指定在请求体匹配期间要忽略的字段。测试生成器从 IR 中提取分页游标字段名称，并在代码生成时将它们传递给模拟服务器。


## 3.43.7
**`(fix):`** 支持 websocket 消息的 `bytes` 参数类型和自定义方法名称。