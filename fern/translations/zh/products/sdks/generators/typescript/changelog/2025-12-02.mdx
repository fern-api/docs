## 3.35.8
**`(chore):`** 添加 `writeUnitTests` 配置选项以控制单元测试生成。`writeUnitTests` 和 `generateWireTests` 现在都默认为 `true` 并独立运行。


## 3.35.7
**`(fix):`** 线路测试现在完全由 `generateWireTests` 自定义配置选项控制，
不再回退到 CLI 的 `writeUnitTests` 标志。


## 3.35.6
**`(chore):`** 从推断认证提供者生成中移除嘈杂的 IR 服务日志。