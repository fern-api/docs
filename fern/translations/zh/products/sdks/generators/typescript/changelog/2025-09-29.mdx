## 3.3.3
**`(fix):`** 如果 `useLegacyExports` 为 `true`，则在 `tests/tsconfig.json` 中引用 `../tsconfig.json` 而不是 `../tsconfig.base.json`。


## 3.3.2
**`(fix):`** 修复非 GitHub 输出模式。

**`(fix):`** 使用 prettier 格式化 GitHub Actions 工作流文件。

**`(fix):`** 添加 .prettierignore 以确保 dist 和临时文件不被格式化。

## 3.3.2-rc1
**`(fix):`** 修复 npm publish 输出模式