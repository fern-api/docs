## 3.59.1
**`(fix):`** 修复生成的 README.md 在 `allowCustomFetcher` 为 `false` 时仍在"Runtime Compatibility"下包含"Customizing Fetch Client"部分的问题。
现在当自定义提取器选项被禁用时，该部分及其代码片段会被条件性地省略。


## 3.59.0
**`(feat):`** 使生成的 WebSocket 套接字类上的 `readyState` 返回 `ReadyState` 枚举（`CONNECTING`、`OPEN`、`CLOSING`、`CLOSED`）而不是原始 `number`，
改善了开发者体验，提供了 IDE 自动完成和自文档化代码。


## 3.58.1
**`(fix):`** 当启用 WebSocket 客户端时，从子包 `exports.ts` 导出生成的 Socket 类。之前只导出了 Client 类，使 Socket 类无法通过公共导出路径访问。


## 3.58.0
**`(fix):`** 为生成的测试模拟服务器代码中的 `mockServerPool` 导出添加显式类型注释，以满足 `--isolatedDeclarations`。


**`(feat):`** 在生成的 vitest 配置中启用 vitest 类型检查，以便在运行 `vitest` 时捕获测试和源文件中的类型错误。