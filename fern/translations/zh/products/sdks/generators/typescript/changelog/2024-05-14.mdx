## 0.18.1
**`(fix):`** 如果指定了 OAuth 环境变量，`clientId` 和 `clientSecret` 参数是可选的。

```ts
export declare namespace Client {
  interface Options {
      ...
      clientId?: core.Supplier<string>;
      clientSecret?: core.Supplier<string>;
  }
  ...
}
```