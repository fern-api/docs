## 0.39.7
**`(chore):`** SDK 现在生成一个 `version.ts` 文件，其中我们导出一个名为 `SDK_VERSION` 的常量。
此常量可以被不同的实用程序用来动态导入版本（例如，如果有人想自定义用户代理）。


## 0.39.6
**`(fix):`** 浏览器客户端现在可以通过 `readable-streams` polyfill 导入流。另外还添加了 webpack 单元测试来验证核心实用程序可以被编译。