## 3.63.1
**`(fix):`** 修复当启用 `noSerdeLayer` 且 IR 示例形状包含的属性少于 JSON
示例（例如内联类型）时线协议测试期望响应不匹配的问题。模拟服务器发送
完整的原始 JSON body，但期望断言是从仅包含属性子集的类型化示例构建的。
当启用 `noSerdeLayer` 时，测试生成器现在使用原始响应 body 作为期望值，
与 SDK 的透传行为保持一致。