## 1.7.0
**`(feat):`** 允许用户指定他们希望生成 SDK 的路径。

以下是如何在 generators.yml 中实现这一功能的示例：
```yml
# In generators.yml
groups:
  ts-sdk:
    generators:
      - name: fernapi/fern-typescript-sdk
        config:
          packagePath: src/package-path
```