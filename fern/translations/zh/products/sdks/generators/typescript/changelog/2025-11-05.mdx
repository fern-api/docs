## 3.28.1
**`(fix):`** 始终使用 vitest.config.mts 以确保所有生成的 SDK 的一致性。这确保 Vitest v3 可以在 CommonJS 和 ESM 包中将配置作为 ESM 加载。


## 3.27.0
**`(feat):`** 在 wire 测试中新增对变量的支持。

## 3.26.0
**`(feat):`** 在生成不包含依赖项的 TypeScript SDK 时，在 package.json 中包含 `"dependencies": {}`。