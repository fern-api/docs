## 0.21.1
**`(feat):`** 在 `Runtime.ts` 中检测 `workerd`（Cloudflare）环境。用于服务器发送事件的 `Stream` 类现在在环境中存在 `TextDecoder` 时优先使用它，以便在 Cloudflare 环境中工作。

## 0.21.0
**`(feat):`** 生成器现在支持 `bigint` 类型。

**`(internal):`** 升级到 IRv46。