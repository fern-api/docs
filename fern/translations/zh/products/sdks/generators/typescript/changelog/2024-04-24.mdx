## 0.16.0-rc0
**`(feat):`** 生成器现在注册代码片段模板，可用于动态 SDK 代码片段生成。

## 0.15.1-rc1
**`(feat):`** 之前对于内联请求导出，我们执行以下操作：

```ts
export { MyRequest } from "./MyRequest";
```

为了使生成的代码兼容 JSR，TS 生成器现在会为请求导出显式添加 `type`。

```ts
export { type MyRequest } from "./MyRequest";
```