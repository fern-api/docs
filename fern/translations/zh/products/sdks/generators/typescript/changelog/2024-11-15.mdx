## 0.42.0
**`(feat):`** 新增了对 `.asRaw()` 的支持，允许用户访问包括头部在内的原始响应数据。例如：

```ts
const response = await client.someEndpoint().asRaw();
console.log(response.headers["X-My-Header"]);
console.log(response.body);
```