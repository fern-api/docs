## 3.24.0
**`(feat):`** 新增对向前兼容枚举的支持。
要启用向前兼容枚举，请在您的 _generators.yml_ 文件中添加以下配置：
```yaml
# 在 generators.yml 中
groups:
  generators:
    - name: fernapi/fern-typescript-sdk
      config:
        enableForwardCompatibleEnums: true
```


## 3.23.0
**`(feat):`** 新增对字节下载响应的支持。


## 3.22.0
**`(feat):`** 新增对 oxfmt 作为格式化器的支持。
这是一个测试版功能，暂不正式支持。


## 3.21.0
**`(feat):`** 新增对 oxlint 作为代码检查器的支持。
这是一个测试版功能，暂不正式支持。


## 3.20.0
**`(feat):`** 在判别联合示例中实现 base 和 extend 属性。


## 3.19.1
**`(fix):`** `mergeHeaders()` 和 `mergeOnlyDefinedHeaders()` 现在不区分大小写。


## 3.19.0
**`(feat):`** 新增对 application/x-www-form-urlencoded 请求体的支持。


## 3.18.0
**`(feat):`** 用户现在可以向客户端选项传入自定义的 `fetch` 函数。