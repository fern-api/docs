## 3.47.0
**`(feat):`** 当 `noSerdeLayer: true` 时，添加对 TypeScript 的 `exactOptionalPropertyTypes` 编译器选项的支持。
可选属性现在生成为 `propName?: Type | undefined` 而不是 `propName?: Type`，
允许用户显式将属性设置为 `undefined`（不仅仅是省略它们）。这通过在添加联合类型之前剥离顶级 undefined 来防止双重联合类型如 `(T | undefined) | undefined`。