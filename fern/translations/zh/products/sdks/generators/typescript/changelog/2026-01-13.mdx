## 3.43.11
**`(chore):`** 将 linter 和格式化工具更新到最新版本。

## 3.43.10
**`(chore):`** 更新 Dockerfile 以仅为默认配置选项预缓存，将镜像大小从 1.45GB 减少到 823MB。以下依赖项不再预缓存：
* jest
* @types/jest
* node-fetch
* @types/node-fetch
* qs
* @types/qs
* readable-stream
* @types/readable-stream
* form-data
* formdata-node
* jest-environment-jsdom
* prettier
* oxfmt
* oxlint
* oxlint-tsgolint
* ts-jest
* form-data-encoder
* fetch-mock-jest

此外，Dockerfile 仅使用 pnpm 进行预缓存，移除了重复的 yarn 缓存。


## 3.43.9
**`(chore):`** 更新 Dockerfile 以使用最新的 generator-cli，改进 reference.md 生成。