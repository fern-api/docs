## 0.11.0
**`(feat):`** Python 现在支持更广泛的文件上传类型

### 修复内容
- Python 现在支持利用列表进行文件上传的 API 规范。之前，Fern 错误地将所有 `list<file>` 类型请求简单地设为 `file`。

### 变更内容
- Python 现在支持更广泛的文件上传类型，镜像底层使用的 `httpx` 库，这些类型归类到一个新类型 `File` 下：

## 0.10.3
**`(fix):`** 修复了与字面量属性相关的几个错误

### 修复内容
- 修复了与字面量属性相关的几个错误。如果字面量用作查询参数、头部、路径参数或请求参数，用户不再需要明确传递它。