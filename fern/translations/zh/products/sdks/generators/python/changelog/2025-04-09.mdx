## 4.13.0
**`(feat):`** 添加对在客户端构造器中覆盖字面全局标头（例如 `version`）的支持。

```python
client = Acme(
  ...
  version="1.0.0",
)
```


## 4.12.1
**`(fix):`** 修复 TypedDicts 中定义的字典类型错误使用 `typing_extensions.NotRequired` 类型的问题。


**`(fix):`** 修复可选 `core.File` 参数导致 `mypy` 失败的问题。


**`(fix):`** 修复 4.12.0 中引入的回归问题，非原始响应客户端端点不遵循允许的方法关键字（例如 `list` 生成为 `list_`）。


## 4.12.0
**`(fix):`** 解决分页端点生成错误端点返回类型的问题。