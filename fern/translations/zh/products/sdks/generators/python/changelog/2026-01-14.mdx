## 4.48.1
**`(fix):`** 修复流式端点的 WireMock stub 生成。当端点同时具有流式和非流式变体时（通过带有 stream-condition 的 x-fern-streaming），生成的 WireMock stub 现在包含请求体匹配条件来区分它们。SSE stub 匹配请求体中的 `stream: true` 并具有更高优先级，确保线缆测试将流式请求正确路由到 SSE stub。