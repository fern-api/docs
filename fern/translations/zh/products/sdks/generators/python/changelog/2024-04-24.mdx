## 1.4.0-rc3
**`(fix):`** 固定 mypy 依赖版本为 1.9.0 以防止引入上游错误

### 已修复的问题
- 在生成的 `pyproject.toml` 中将 `mypy` 开发依赖设置为 `1.9.0`。这可以防止上游 `mypy` 错误影响用户构建。请注意，这只是一个开发依赖，因此不会影响 SDK 的行为。
- 暂时禁用单元测试生成。

### 更改内容
- 改进：为所有 `httpx` 请求参数使用命名参数。