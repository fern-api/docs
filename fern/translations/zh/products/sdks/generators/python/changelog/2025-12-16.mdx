## 4.46.4-rc1
**`(fix):`** 修复当基础 URL 包含路径前缀时的 URL 路径剥离问题。

## 4.46.2
**`(fix):`** 修复使用 `config.pydantic_config.use_provided_defaults` 时字符串默认值中换行符的处理。

## 4.46.1
**`(fix):`** 修复可选 expires_in 字段的 OAuth 令牌处理，使用默认回退。

## 4.46.0
**`(feat):`** FastAPI Python 代码生成现在通过 `typing.Annotated[...]` 发出参数标记，并使用标准 Python 默认值以获得更好的 FastAPI/Pydantic 兼容性。