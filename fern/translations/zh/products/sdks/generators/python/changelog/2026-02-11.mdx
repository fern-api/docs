## 4.56.0
**`(feat):`** 添加对基于 URI 和路径的分页的支持。


## 4.55.5
**`(fix):`** 在生成期间计算 Docker Compose 项目名称，以避免运行时目录名称中的无效字符（例如 `.seed`）。


**`(chore):`** 在模块级别缓存客户端构造函数签名检查，并在生成的 wire 测试 conftest 中使用 httpx 而不是 requests 进行 WireMock 管理调用。