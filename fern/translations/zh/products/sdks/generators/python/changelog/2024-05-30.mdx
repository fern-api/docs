## 2.7.0
**`(internal):`** 改进：生成器现在在每个生成的代码片段旁边输出 `exampleId`，这样我们就可以将代码片段与相关示例关联起来。这对于从 Fern 的 API 检索示例并确保在生成的文档中显示多个代码片段非常有用。

## 2.6.0
**`(chore):`** 支持向生成的 `pyproject.toml` 添加可选依赖项和额外组件。

### 变更内容
- 改进：支持向生成的 `pyproject.toml` 添加可选依赖项和额外组件。要使用此配置，请添加以下内容：

## 2.5.7
**`(fix):`** 测试现在为 `expected_types` 变量添加了类型注解。