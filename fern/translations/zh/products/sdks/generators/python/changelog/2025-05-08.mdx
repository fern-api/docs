## 4.18.6
**`(fix):`** 修复字符串枚举生成，通过自动转义包含引号的枚举值来正确处理，确保单引号和双引号字符串字面量的 Python 语法有效。


## 4.18.5
**`(fix):`** 支持请求的 application/x-www-form-urlencoded 内容类型，允许在 API 调用中正确编码表单数据。


## 4.18.4
**`(fix):`** 修复 requirements.txt 生成以正确处理依赖版本约束，确保兼容性标记格式正确。