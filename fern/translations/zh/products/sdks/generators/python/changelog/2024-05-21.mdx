## 2.4.0-rc0
**`(fix):`** Python SDK 生成器现在在不使用字符串连接的地方（如客户端生成命名中）使用安全名称，因此这将更新模块和参数名称。

## 2.3.4
**`(fix):`** 当 `inline_request_params` 设置为 `True` 时，代码片段和单元测试现在正确写入可选请求体。

### 已修复的问题
- 当 `inline_request_params` 设置为 `True` 时，代码片段和单元测试现在正确写入可选请求体。以前生成器编写的代码片段内联了这些参数，这与生成的 SDK 本身不匹配。

## 2.3.3
**`(fix):`** 内联主体参数现在通过为请求对象名称添加前缀来与头部和查询参数在命名上解决冲突。

## 2.3.2
**`(fix):`** 查询编码器现在正确处理 none 值

### 已修复的问题
- `pyproject.toml` 生成器现在以对 `tool.poetry` 有效的格式编写作者，而不仅仅是 `project`
- 查询编码器现在正确处理 none 值

## 2.3.1
**`(fix):`** 当指定时，`pyproject.toml` 生成器现在包含项目 URL。

## 2.3.0
**`(chore):`** 用户现在可以指定将出现在其 PyPI 记录中的信息。