## 4.2.7-rc1
**`(fix):`** 当使用 `include_union_utils` 配置标志时，Pydantic 模型将不再在 `update_forward_ref` 调用中包含传递依赖，因为这些应该不是必需的。这有效地限制了 4.0.0-rc5 中引入的更改，该更改包括改进 Pydantic 模型中循环引用处理的修改。


## 4.2.7-rc0
**`(fix):`** 动态标头供应商，如 OAuth 提供程序中使用的，现在在每次请求时调用，而不仅仅是第一次。这是在早期版本中引入的回归，现已修复。由于此修复，`refresh_token` 现在正确刷新。