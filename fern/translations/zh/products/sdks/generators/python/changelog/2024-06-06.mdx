## 2.9.3
**`(fix):`** 判别联合的代码片段模板现在指定了 `template_input` 属性，这是实际查看判别联合实例化代码片段所必需的。

## 2.9.2
**`(fix):`** 降级 mypy，这样我们就可以在所有文件上运行它，而不用担心它们的 pydantic 错误

### 修复内容
- 降级 mypy，这样我们就可以在所有文件上运行它，而不用担心它们的 pydantic 错误
- 为响应变量添加类型提示

## 2.9.1
**`(fix):`** SDK 从请求中移除未设置的查询参数（这是 2.8.0 中客户端迁移的回归）

### 修复内容
- SDK 从请求中移除未设置的查询参数（这是 2.8.0 中客户端迁移的回归）
- SDK 修复了向 http 客户端传递 `files` 参数的类型（这是 2.8.0 中客户端迁移的回归）