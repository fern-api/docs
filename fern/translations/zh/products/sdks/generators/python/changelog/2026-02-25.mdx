## 4.60.0
**`(feat):`** 在 WebSocket 通道上支持 `connectMethodName`。当通道通过 `x-fern-sdk-method-name` 指定自定义连接方法名时，生成的 SDK 使用该名称（转换为 snake_case），而不是默认的 `connect`。


## 4.59.4
**`(fix):`** 修复了名称中包含连字符的组织的 wire 测试导入生成。在可用时使用自定义配置的 `package_name`，并在后备模块路径中自动将连字符转换为下划线，以生成有效的 Python 导入语句。