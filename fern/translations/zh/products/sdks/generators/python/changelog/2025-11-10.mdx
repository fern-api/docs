## 4.35.2
**`(fix):`** 为循环引用重新添加 F401 忽略的导入。

## 4.35.1
**`(fix):`** 生成的 Python SDK 在 API 文档或枚举值包含反斜杠时不再显示 SyntaxWarning。

## 4.35.0
**`(feat):`** 使用 Pydantic 的 Field(discriminator=...) 添加自动判别联合支持以提高序列化性能。基准测试显示通过消除顺序变体尝试并启用 O(1) 变体选择实现了 2 倍的加速。