## 3.4.0
**`(internal):`** 生成器代码现在使用 Pydantic V2，生成的代码无变化。

### 已更改的内容
- 内部：SDK 生成器现在已升级为内部使用 Pydantic V2。注意生成的代码没有变化，但是通过利用 Pydantic V2，您应该注意到 `fern generate` 时间的改善。

## 3.3.4
**`(chore):`** 解决生成的单元测试中的多个问题。

### 已修复的问题
- 当类型为 `text`（非 JSON）时，期望空结果的生成测试现在适当地期望空字符串而不是 `None`。

### 已更改的内容
- 改进：别名字面量现在也在 Pydantic 模型中设为默认值，而之前只有直接字面量被设为默认值。
- 改进：代码片段现在在函数和模型中提供可选字面量。

## 3.3.3
**`(fix):`** 生成器现在允许您扩展别名类型（只要它们是对象）。

## 3.3.2
**`(fix):`** 3.3.1 版本中引入的 readme 生成回归问题。

## 3.3.1
**`(fix):`** 生成的 README 现在正确地将 RequestOptions 引用为 TypedDicts。