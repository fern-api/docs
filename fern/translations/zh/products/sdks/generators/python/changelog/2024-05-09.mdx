## 2.0.0
**`(break):`** 发布 Python SDK 生成器版本 2，更新默认配置。

### 更改内容
- Python SDK 现在是主版本 2，没有实质性的逻辑更改，但默认配置已更改。要在不中断的情况下进行此升级，请将以下配置添加到您的 `generators.yml` 文件中：

## 1.7.0-rc0
**`(chore):`** 您现在可以为 `pyproject.toml` 声明新的 Python 版本范围，这将为您的 pip 包声明新的版本范围。

## 1.6.0-rc0
**`(chore):`** 您现在可以从 `generators.yml` 文件指定开发依赖