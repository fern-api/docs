## 2.13.1-rc0
**`(fix):`** 如果请求利用其他字段，Python SDK 现在不会通过 JSON 或数据发送额外属性。

### 修复内容
- 如果请求利用其他字段，Python SDK 现在不会通过 JSON 或数据发送额外属性。

### 变更内容
- 改进：Python SDK 现在将单元测试复制到生成的 SDK 中以进行额外的单元测试（与线格式测试分开）。

## 2.13.0-rc0
**`(internal):`** Python SDK 生成器现已升级到 IR V49。

## 2.10.2
**`(fix):`** SDK 现在处理像 `[DONE]` 这样的流终止序列。

### 修复内容
- SDK 现在处理像 `[DONE]` 这样的流终止序列。这是 LLM 提供商通信流已结束的典型方式。

## 2.10.1
**`(fix):`** 改进 SDK 以不向 SDK 用户泄露 `JSONDecodeError`。相反，将抛出带有响应文本内容的 `ApiError`。

## 2.10.0
**`(feat):`** 添加对更高质量 `README.md` 生成的支持。

## 2.9.10
**`(fix):`** 生成器现在只有在成功创建 readme 时才在 pyproject.toml 中指定 readme 位置。