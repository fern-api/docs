## 4.45.9
**`(fix):`** 修复空查询参数剥离现有 URL 查询字符串的问题。当 `params` 和 `additional_query_parameters` 都为空时，httpx 现在接收 `params=None` 而不是 `params=[]`，保留 URL 中已存在的任何查询参数（例如，分页游标如 `?after=123`）。

## 4.45.8
**`(fix):`** 确保根客户端始终暴露 headers 参数并将其转发给底层 ClientWrapper。

## 4.45.7
**`(fix):`** 修复客户端包装器实例化以使用正确的承载令牌参数名称，防止在配置自定义认证参数名称时出现不正确的 `token=` 引用。