## 4.46.13
**`(fix):`** 修复 Python SDK 导入生成，通过从规范声明派生类型路径来避免无效的 `._` 模块段。

## 4.46.12
**`(fix):`** 支持 `bytes` 作为 websocket 通道方法的响应类型。

## 4.46.11
**`(fix):`** 线路测试生成器现在跳过未定义的示例值，并通过 `headers` 或 `httpx_client` 以构造函数兼容的方式注入 `X-Test-Id`。