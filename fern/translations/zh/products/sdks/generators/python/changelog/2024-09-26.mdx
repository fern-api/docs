## 4.2.7-rc4
**`(fix):`** 生成的 README 现在在示例代码片段中包含请求选项参数所需的参数名称。

## 4.2.7-rc3
**`(fix):`** 现在，当向 Fern 发送代码片段模板时，生成器不会尝试将可能丢失的 ID 强制转换到 `api_definition_id` 字段中。这曾经是错误日志 `Failed to upload snippet templates to FDR, this is ok: one of the hex, bytes, bytes_le, fields, or int arguments must be given` 的原因。


## 4.2.7-rc2
**`(fix):`** Pydantic 实用程序现在正确处理您有一个 Pydantic 模型，其中包含作为字段的 Pydantic 模型列表，而这些模型具有文字的情况。实际上，`deep_union_pydantic_objects` 现在处理对象列表并适当地合并它们。