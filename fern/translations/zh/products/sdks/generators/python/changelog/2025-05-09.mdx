## 4.19.3
**`(fix):`** 通过正确索引响应数据来修复原生客户端中的分页支持，确保带分页的原生客户端端点能够正确迭代分页结果。


## 4.19.2
**`(fix):`** 通过向 sse.data 添加适当的类型转换来修复 SSE 流式响应处理，确保流处理期间正确的数据类型转换。


## 4.19.1
**`(fix):`** 通过正确处理 Pydantic v1 和 v2 兼容性的类型注释来修复 pydantic_utilities.py 中的 mypy 错误，确保类型检查在不同 Pydantic 版本中正确通过。


## 4.19.0
**`(feat):`** 添加生成包含容器中包装的前向引用的正确 Pydantic 模型的支持（例如 list、optional 等）