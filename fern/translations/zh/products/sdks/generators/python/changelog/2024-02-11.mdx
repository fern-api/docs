## 0.9.1
**`(fix):`** 从 SDK 函数签名中移除字面量，因为它们对最终用户来说不可修改

### 修复内容
- 从 SDK 函数签名中移除字面量，因为它们对最终用户来说不可修改。
- 确认 `File` 属性的可选性，之前我们要求所有 `File` 类型的输入，即使它们在 OpenAPI 或 Fern 定义中被指定为可选。现在，我们检查参数是否为必需，如果不是则将参数设为可选。

## 0.9.0
**`(feat):`** SDK 生成器现在支持白标化

### 新功能
- SDK 生成器现在支持白标化。当启用此功能时，生成的代码中不会出现 Fern 的任何提及。