## 4.23.1
**`(chore):`** 将 pydantic-core 依赖升级到 >=2.18.2 以确保与较新的 pydantic 版本兼容并提高性能。


## 4.23.0
**`(feat):`** 在客户端构造函数的顶层添加对自定义头参数的支持。

```python
client = Client(
    headers={"X-Custom-Header": "custom-value"}
)
```