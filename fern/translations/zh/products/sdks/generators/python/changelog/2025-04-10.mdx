## 4.14.1
**`(fix):`** 生成的 GitHub 工作流现在使用 `ubuntu-latest` 而不是 `ubuntu-20.04`


## 4.14.0
**`(feat):`** 添加对从流端点接收原始响应的支持。


**`(feat):`** 添加对从生成的 `__init__.py` 文件中排除类型的支持。

```yaml
config:
  exclude_types_from_init_exports: true
```