## 4.19.6
**`(fix):`** 修复 python 客户端中与自动分页相关的 mypy 错误。


## 4.19.5
**`(fix):`** 通过在原生客户端中将流方法重命名为 _stream 来修复流式端点中的命名冲突，防止与可能同样命名为 "stream" 的端点参数发生冲突，同时维护流式响应的正确功能。


## 4.19.4
**`(feat):`** 通过在尝试将可选文件参数包含在请求中之前正确检查 None 值，修复 httpx 多部分表单数据请求中对可选文件参数的处理，防止未提供可选文件参数时出现错误。