## 1.1.0-rc3
**`(fix):`** 跳过验证代码现在按预期工作。

### 已修复的问题
- 跳过验证代码有许多修复以及反映这些更新的测试。

## 1.1.0-rc2
**`(fix):`** 生成器现在使用后缀换行符写入跳过验证的 `cast`，以便代码编译。

## 1.1.0-rc1
**`(fix):`** 如果 Fern 不拥有生成完整包（例如在本地生成中），生成器不再尝试创建版本文件。

### 已修复的问题
- 如果 Fern 不拥有生成完整包（例如在本地生成中），生成器不再尝试创建版本文件。对包设置进行相关更改太令人困惑，在本地生成中可以说甚至不需要。