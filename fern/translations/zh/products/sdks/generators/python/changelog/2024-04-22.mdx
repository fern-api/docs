## 1.4.0-rc1
**`(feat):`** Python SDK 生成器现在支持客户端凭据流的 OAuth 客户端生成。

## 1.4.0-rc0
**`(chore):`** 生成的客户端现在默认跟随重定向。

### 已更改的内容
- 默认生成的客户端跟随重定向，这有效地将 `follow_redirects_by_default` 标志翻转为 `True`，可以使用以下配置恢复：

## 1.3.1-rc0
**`(fix):`** Python SDK 生成器现在在将标头转换为字符串之前检查确保标头不为 null。

## 1.3.0-rc1
**`(internal):`** 为 Python 片段模板生成添加日志记录。