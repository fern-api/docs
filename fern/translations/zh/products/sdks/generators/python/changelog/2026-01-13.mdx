## 4.47.1
**`(chore):`** 更新 Dockerfile 以使用最新的 generator-cli，改进 reference.md 生成。

## 4.47.0
**`(feat):`** 为所有请求添加 `X-Fern-Runtime`（例如 `python/3.12.7`）和 `X-Fern-Platform`（例如 `darwin/25.2.0`）遥测标头，以获得更好的运行时/平台可观察性。