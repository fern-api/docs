## 4.46.10
**`(fix):`** 修复线路测试生成以使用端点 ID 进行查找而不是路径匹配。这修复了当多个命名空间具有相同 HTTP 方法和路径模式的端点（例如 POST /v2/Services）时，线路测试会调用错误的客户端方法的错误。

## 4.46.9
**`(fix):`** 修复线路测试生成以在生成方法参数时过滤掉 nop 值。这防止了当示例包含无效枚举值或未定义属性时出现类似 `param=` 而没有值的无效 Python 语法。