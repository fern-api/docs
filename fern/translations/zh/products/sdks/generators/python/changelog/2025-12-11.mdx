## 4.45.6
**`(fix):`** 通过使用单个共享 WireMock 插件修复在 `pytest-xdist` 下失败的线路测试。

## 4.45.5
**`(fix):`** 修复文件上传条件检查，使用 wire_value 而不是 snake_case 参数名称，这解决了当 API 字段名称与 Python 参数名称不同时（例如 `MyFile` vs `my_file`）导致的 mypy "名称未定义" 错误。

## 4.45.4
**`(fix):`** 修复偏移分页以在计算下一页偏移时处理 None 项目。