## 0.2.0
**`(feat):`** 添加对幂等性头部的支持

### 更新内容
- Ruby enum 构造现在使用类常量而不是哈希来支持更好的自动完成
- 判别联合不再封装在父对象中，而是任何依赖于判别联合的字段或参数现在都明确引用成员类型，以支持更好的自动完成。
- 不判别联合不再允许作为哈希输入到 SDK 函数中，这也是为了支持更好的自动完成。
- 生成的 Ruby SDK 现在支持幂等性头部，用户可以在 RequestOptions 对象中指定幂等性头部