## 0.7.0-rc5
**`(feat):`** 针对 0.7.0-rc4 中相同问题的额外修复（在 0.7.0 RC 中引入的回归，其中令牌前缀从请求中被丢弃）。

## 0.7.0-rc4
**`(feat):`** 修复在 0.7.0 RC 中引入的回归，其中令牌前缀从请求中被丢弃。

## 0.7.0-rc3
**`(fix):`** 模块引用现在在生成的 SDK 中保持一致，这是之前版本的回归。

## 0.7.0-rc2
**`(feat):`** ruby 生成器现在将类型嵌套在类型模块下以避免命名冲突，这在配置标志后面

## 0.7.0-rc1
**`(feat):`** 解决可迭代类型中的序列化问题

### 修复内容
- 嵌套循环使用不同的变量名来避免冲突
- 嵌套循环在后续循环之前不调用 to_json

## 0.6.1
**`(internal):`** 发布生成器

## 0.6.1-rc0
**`(internal):`** 改进 Ruby 生成器中的日志记录

## 0.6.0-rc1
**`(fix):`** 修复有时 parsed_json 变量不会被实例化的回归，因此在生成的代码中会有空引用

## 0.6.0-rc0
**`(feat):`** 为 Ruby SDK 引入代码片段和示例

## 0.5.0-rc2
**`(fix):`** 在迭代可迭代响应之前调用 JSON.parse

## 0.4.0
**`(feat):`** 生成的 SDK 现在包含一个 rakefile 来运行 `test` 目录中任何以 `test_` 为前缀的测试

### 新功能
- 生成器现在创建一个 rakefile 来运行 `test` 目录中任何以 `test_` 为前缀的测试。还在 CI 中添加了一个步骤来运行这些测试。现在运行的虚拟测试也提供了对 gem 构建健康状况的理智检查，即使没有添加测试，因为 gem 被导入。