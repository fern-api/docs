## 1.0.8
**`(fix):`** 记录错误后重新抛出 README.md 和 reference.md 生成失败的错误。
文档生成错误现在会中断生成流程，因此被视为重要的需要修复的问题。


## 1.0.7
**`(fix):`** 修复动态代码片段以在生成的代码示例中包含全局请求头。
以前，通过 `x-fern-global-headers` 配置的必需全局请求头（例如 `X-Organization-ID`）在动态代码片段输出中缺失。同时修复了请求头值查找以使用线上值而不是不存在的属性。