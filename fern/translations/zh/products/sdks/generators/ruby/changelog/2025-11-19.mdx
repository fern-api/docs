## 1.0.0-rc39
**`(feat):`** 为生成的 SDK 代码中的方法参数添加 YARD 文档注释。


## 1.0.0-rc38
**`(fix):`** 修复路由参数和多部分文件上传属性在 Ruby 代码中使用 snake_case 名称而不是 camelCase 线值。
Ruby 参数键现在遵循 Ruby 命名约定（例如 `params[:payment_id]` 而不是 `params[:paymentId]`），同时为 HTTP 请求保留原始线值。


## 1.0.0-rc37
**`(chore):`** 升级生成器 CLI 版本以发布新的 Docker 镜像。