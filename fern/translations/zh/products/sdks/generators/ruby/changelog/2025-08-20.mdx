## 1.0.0-rc17
**`(fix):`** 修复引用请求方法。

## 1.0.0-rc16
**`(fix):`** 修复子包客户端生成。

## 1.0.0-rc15
**`(fix):`** 生成嵌套子包客户端。

## 1.0.0-rc14
**`(fix):`** 修复无参数端点方法。

## 1.0.0-rc13
**`(feat):`** 实现包装请求端点。

## 1.0.0-rc12
**`(fix):`** 支持多部分表单上传。

## 1.0.0-rc11
**`(fix):`** 在生成 SDK 后安装依赖项以生成 Gemfile.lock。

## 1.0.0-rc10
**`(fix):`** 生成 Rubocop 文件并自动纠正生成的代码。