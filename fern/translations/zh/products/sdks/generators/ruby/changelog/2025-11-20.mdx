## 1.0.0-rc42
**`(fix):`** 改进生成的 Ruby SDK 代码中的 YARD 文档语法。


## 1.0.0-rc41
**`(fix):`** 移除 RawClient 中的死代码并遵循 max_retries 参数。max_retries 参数现在被正确使用，而不是硬编码为 0。


## 1.0.0-rc40
**`(fix):`** 重新排列 YARD 标签以遵循标准约定（@param 在 @option 之前）。