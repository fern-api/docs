## 1.0.0-rc47
**`(fix):`** 修复 request_options[:base_url] 被忽略的问题。
RawClient.build_url 方法现在在提供每个请求的 base_url 覆盖时正确使用它。


## 1.0.0-rc46
**`(fix):`** 改进 README 片段生成：对环境引用使用 SCREAMING_SNAKE_CASE，
对嵌套对象属性名称使用 snake_case，并过滤掉空/未定义的值以
避免像 "param: ," 这样的语法错误。


## 1.0.0-rc45
**`(feat):`** 添加对多 URL 环境的支持。每个环境具有多个基础 URL 的 API
（例如，单独的 api 和 identity URL）现在使用
每个环境的第一个基础 URL 生成适当的 Environment 类。


## 1.0.0-rc44
**`(fix):`** 移除 README 生成中对空配置的错误。