## 0.3.3
**`(internal):`** 利用共享的生成器通知和配置解析逻辑。

## 0.1.0-rc0
**`(chore):`** 放宽生成的 SDK 中的 Faraday 依赖

### 更新内容
- 放宽生成的 SDK 中的 Faraday 依赖，现在我们支持 Faraday 1.x，同时继续支持相同的上界（特别支持最新的主版本）。
- 发布次要版本，因为 Ruby 生成器现在正在测试版中使用！

## 0.0.6
**`(feat):`** 如果提供了许可证文件，现在会在 gem 配置中指定许可证文件

## 0.0.5
**`(fix):`** 解决 SDK 中的解析问题

### 修复内容
- 块参数使用中的语法错误：我们现在确保块参数是函数的最终参数
- 为子包添加属性：以前子包上的属性没有被暴露
- 确保 from_json 中的可选属性仅在存在时才被解析

## 0.0.4
**`(fix):`** 确保文件以 gem 名称路径写入而不是客户端名称，并解决 Fern 头部之一的字符串转义问题

## 0.0.3
**`(fix):`** 解决了生成代码中以前包含的许多拼写错误和其他问题

## 0.0.2
**`(feat):`** 在 `generators.yml` 中支持 rubygems 输出类型