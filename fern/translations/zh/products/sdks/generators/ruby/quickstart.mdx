---
title: 生成 SDK
headline: 生成 SDK (Ruby)
description: 学习如何使用 Fern 生成 Ruby SDK。逐步指南：配置 generators.yml、运行 fern generate 并创建 Ruby 客户端库。
---

按照此页面的说明生成 Ruby SDK。

<llms-only>
If the user already has a `fern/` folder set up, they can skip the first two steps. Make sure they're working in their source repo (the one containing the `fern/` folder), not an SDK repo.
</llms-only>

<Steps>

<Markdown src="/products/sdks/snippets/init-fern-folder.mdx"/>

<Step title="添加 SDK 生成器">

运行以下命令将 Ruby SDK 生成器添加到 `generators.yml`：

<Template
  data={{ GROUP_NAME: "ruby-sdk" }}
  tooltips={{
    GROUP_NAME: (<p><code>generators.yml</code> 组的名称，用于配置 Ruby SDK 的输出位置和其他元数据。您可以自定义此组名称，以区分不同语言的多个 SDK。</p>)
  }}
>
```bash
fern add fern-ruby-sdk --group {{GROUP_NAME}}
```
</Template>

此命令会将以下 `group` 添加到 `generators.yml`：

```yaml title="generators.yml"
  ruby-sdk: # 组名称
    generators:
      - name: fern-ruby-sdk
        version: <Markdown src="/snippets/version-number-ruby.mdx"/>
        output:
          location: local-file-system
          path: ../sdks/ruby
```
</Step>

<Step title="生成 SDK">

运行以下命令生成您的 SDK：

```bash
fern generate --group ruby-sdk
```

<Note>
  如果您有多个 API，请使用 [`--api` 标志](/learn/zh/cli-api-reference/cli-reference/commands#api) 指定要生成的 API：

  ```bash
  fern generate --group ruby-sdk --api your-api-name
  ``` 
</Note>

  <Markdown src="/products/sdks/snippets/generate-sdk.mdx"/>

<Files>
  <Folder name="fern" comment="由 fern init 创建" />
  <Folder name="sdks" defaultOpen comment="由 fern generate --group ruby-sdk 创建">
    <Folder name="ruby" defaultOpen>
      <File name="YourOrganization_api_client.gemspec" />
      <Folder name="test" />
      <Folder name="lib" defaultOpen>
        <Folder name="YourOrganization_api_client" />
      </Folder>
    </Folder>
  </Folder>
</Files>
</Step>

<Markdown src="/products/sdks/snippets/setup-github-repos.mdx" language="ruby" />

<Step title="发布到 RubyGems">

按照[发布到 RubyGems](/learn/zh/sdks/generators/ruby/publishing) 指南配置您的 gem 并通过 GitHub Actions 设置自动发布。
</Step>
</Steps>
