## 1.16.2
**`(feat):`** 添加 `root-client-class-access` 配置选项来控制根客户端类的访问修饰符。
`root-client-class-access` 默认为 `public`，但可以设置为 `internal` 使根客户端类变为内部类。


**`(fix):`** 将 `csharpier` 格式化工具锁定到 `0.*` 版本以避免格式化工具的重大变更。