## 1.9.27
**`(fix):`** 修复枚举和时间戳类型的 Protobuf 映射器。


## 1.9.26
**`(fix):`** 当当前命名空间中存在名为 `System` 的类型时，在 `OneOfSerializer` 中使用 `global::System.Type` 而不是 `System.Type`。


## 1.9.25
**`(fix):`** 在生成的代码中使用 `global::System.Type` 而不是 `System.Type`，以避免当前命名空间中存在名为 `System` 的类型时的潜在命名冲突。


## 1.9.24
**`(fix):`** 在生成的客户端引用中使用原始的 gRPC 服务名称，而不是应用任何大小写转换。


## 1.9.23
**`(fix):`** 修复方法返回类型，当方法返回响应时使用不限定的 `Task<T>` 而不是 `System.Threading.Tasks.Task<T>`。


## 1.9.22
**`(feat):`** 在生成的 README.md 文件中生成分页部分。


**`(feat):`** 现在可以在 `Page<T>` 实例上使用 `foreach(var item in page)`。