## 1.9.21
**`(fix):`** 支持查询参数和请求头中的字面量。例如，如果包装请求中的字段具有 "foo" 的字面量值，我们现在会将该值设置为字段的默认值。
请注意，包装请求只是包含主体属性的属性集合。


## 1.9.20
**`(fix):`** 支持包装请求中的字面量。例如，如果包装请求中的字段具有 "foo" 的字面量值，我们现在会将该值设置为字段的默认值。
请注意，包装请求只是包含主体属性的属性集合。


## 1.9.19
**`(fix):`** 修复枚举路径参数的序列化。以前，枚举路径参数使用其 C# 枚举名称进行序列化，但现在它们
使用其线上值正确序列化。


## 1.9.18
**`(fix):`** 修复枚举路径参数的序列化。以前，枚举路径参数使用其 C# 枚举名称进行序列化，但现在它们
使用其线上值正确序列化。


## 1.9.17
**`(fix):`** 支持必需的字面量。例如，如果字段具有 "foo" 的字面量值，我们现在会将该值设置为字段的默认值。


## 1.9.16
**`(fix):`** 现在在进行 API 请求时会发送作为请求头的必需客户端参数。例如，如果需要承载令牌来
实例化客户端，我们将确保在进行 API 请求时发送它。


## 1.9.15
**`(fix):`** 将非分页端点从 internal 更改为 private，以避免在可访问内部的测试项目中出现歧义引用。

**`(fix):`** 修复名为 `Equals` 的枚举值总是被转换为 `"Equals"` 而不是其正确线上值的问题。


**`(feat):`** 通过并行化测试并将 HTTP 请求重试延迟设置为 0 来提高测试性能。