## 2.1.0
**`(feat):`** 向自定义分页器工厂传递自定义分页器上下文。上下文包含发送请求方法、初始 HTTP 请求、客户端选项和请求选项。
SDK 作者可以利用此上下文改进他们的自定义分页实现。


## 2.0.3
**`(fix):`** 模拟服务器测试是基于被测试的端点名称生成的。
当多个端点具有相同名称时，它们会相互覆盖，导致只有其中一个被测试。
模拟服务器测试现在使用命名空间，并按照与生成端点相同的结构写入文件系统。


**`(fix):`** 更新类型引用逻辑，以考虑当前命名空间、其他命名空间以及可能发生冲突的更多场景。