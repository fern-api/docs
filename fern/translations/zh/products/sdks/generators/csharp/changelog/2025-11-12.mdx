## 2.9.0
**`(feat):`** 将 C# SDK 生成器 Docker 镜像升级到使用 .NET 10 SDK 基础镜像。
* 提高了包还原和构建性能
* .NET 10 使用 .slnx 解决方案文件而不是 .sln 文件，这些文件更易读且生成具有确定性
* 此更改不会影响生成的 SDK 的目标框架


## 2.8.0
**`(feat):`** .github/workflows/ci.yml 文件更改：
* 将 actions/checkout 更新到 v5
* 将 actions/setup-dotnet 更新到 v5
* 使用 .NET 10 SDK
* 拆分还原和构建步骤
* 除非运行测试，否则不构建测试项目