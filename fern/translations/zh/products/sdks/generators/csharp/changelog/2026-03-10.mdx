## 2.24.4
**`(fix):`** 修复当设置 `use-undiscriminated-unions: true` 时未区分联合的动态代码片段生成。之前，代码片段总是发出 `OneOf<A, B, C>`，无论配置设置如何。现在，当启用该设置时，代码片段使用 SDK 的生成联合包装器类型（例如 `MixedType`），而不是匹配实际的 SDK 类型并生成可编译的代码。


## 2.24.3
**`(fix):`** 在记录 README.md 和 reference.md 生成失败后重新抛出错误。文档生成错误现在会中断生成流程，因此被视为需要修复的重要问题。


## 2.24.2
**`(fix):`** 修复动态代码片段以在生成的代码示例中包含全局头部。之前，通过 `x-fern-global-headers` 配置的必需全局头部（例如 `X-Organization-ID`）在动态代码片段输出中缺失。同时修复头部值查找以使用线路值而不是不存在的属性。