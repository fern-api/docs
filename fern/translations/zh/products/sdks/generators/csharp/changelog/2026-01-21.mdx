## 2.17.0
**`(feat):`** 尊重为请求参数值设置的默认值。

当在 generators.yml 中配置 `use-default-request-parameter-values: true` 时，SDK 将为具有默认值的记录属性生成 `= default` 形式的初始化器。任何具有默认值的属性也将去掉 'required' 关键字（如果存在），因为用户应该被允许在初始化期间省略它并依赖默认值。