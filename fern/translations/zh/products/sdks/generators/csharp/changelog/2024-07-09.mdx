## 0.1.0
**`(feat):`** 添加对 .NET Standard 2.0 和 .NET Framework 4.6.2 的目标。

**`(fix):`** 避免 `StringEnumSerializer` 中的重复键错误。

**`(fix):`** 修复根客户端请求导致生成失败的错误。

**`(fix):`** 正确处理环境值和字面头部名称。

**`(internal):`** 改进构造函数参数和其他小修复。