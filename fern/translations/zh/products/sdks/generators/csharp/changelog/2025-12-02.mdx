## 2.9.7
**`(fix):`** Wire 测试现在完全由 `enable-wire-tests` 自定义配置选项控制，
不再使用 CLI 的 `writeUnitTests` 标志。


## 2.9.6
**`(fix):`** 修复 getGitHubConfig 在缺少发布配置时返回带有 undefined 值的 RawGithubConfig 而不是抛出错误，与 TypeScript/Python 生成器行为保持一致。