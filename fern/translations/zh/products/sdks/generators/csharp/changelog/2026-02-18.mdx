## 2.21.0
**`(feat):`** 在生成的 `///` 注释中将 OpenAPI 描述中的 HTML 标签转换为符合 C# 习惯的 XMLDoc 等效标签（`<code>` → `<c>`，`<a href>` → `<see href>`，`<ul><li>` → `<list type="bullet"><item>`，`<strong>` → `<b>`，`<em>` → `<i>`，`<p>` → `<para>`，`<pre><code>` → `<code>`，`<tt>` → `<c>`）。