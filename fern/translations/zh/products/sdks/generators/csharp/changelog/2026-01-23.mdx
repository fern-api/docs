## 2.17.2
**`(fix):`** 修复模拟服务器测试，从期望的请求 JSON 中排除只读属性。

以前，模拟服务器测试在期望的请求 JSON 中包含只读属性（标记为 `access: read-only`），导致测试失败，因为 SDK 在序列化期间正确排除了这些属性。此修复在所有嵌套级别（包括字典、列表和嵌套对象内）过滤掉只读属性。


## 2.17.1
**`(fix):`** 在 OptionalAttribute 和 NullableAttribute 中的 Attribute 添加 global::System。