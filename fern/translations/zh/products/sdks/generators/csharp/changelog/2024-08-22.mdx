## 1.3.0
**`(internal):`** 无更改。

## 1.3.0-rc2
**`(internal):`** 修复生成的 `RawClient` 中的警告。

**`(internal):`** 为各种数字类型使用更简单的原始实例化。

## 1.3.0-rc1
**`(feat):`** 生成 `ToString` 方法以写入对象的 JSON 格式。

**`(feat):`** 生成代码片段作为示例文档。

## 1.3.0-rc0
**`(feat):`** 添加发送 `User-Agent` 标头的支持。

**`(internal):`** `RawClient` 现在支持 `ClientOptions` 和 `RequestOptions` 类型中的 HTTP 标头。

**`(feat):`** 添加对 `package-id` 配置的支持，用于控制 NuGet 中包的名称。

**`(feat):`** 通过 `generate-mock-server-tests` 配置选项添加模拟服务器测试的支持。

**`(internal):`** 在请求中省略 `null` 属性值。

**`(fix):`** 修复包含标头或查询参数的包装请求不发送请求主体的错误。

**`(fix):`** 修复枚举、日期和日期时间有时不能正确序列化为查询参数和标头的错误。

**`(feat):`** 添加对 `read-only-memory-types` 配置的支持。

**`(feat):`** 将 `CancellationToken` 参数作为每个端点方法的最后一个参数添加。

**`(feat):`** 添加对 gRPC/Protobuf 端点的支持。