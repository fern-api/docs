## 1.12.0-rc18
**`(fix):`** 使包装请求（包含 body 属性）与普通 body 请求的行为保持一致。
以前，即使 JSON 配置被配置为省略 `null` 值，包含 body 属性的包装请求也不会省略 `null` 值。


**`(fix):`** 修复在序列化过程中带有 `[JsonIgnore]` 的必需属性抛出错误的问题。


**`(feat):`** 通过减少对 `JsonSerializer` 的依赖并更多地使用 `ToString()` 来改进查询字符串值到字符串转换的性能。