## 2.63.2
**`(fix):`** 修复当示例值已经是列表时的多值查询参数代码片段。此前，这会在模拟服务器测试、参考文档和代码片段中产生无法编译的 list-of-list 初始化器，例如 `[new List<T>() { value }]`。


## 2.63.1
**`(fix):`** 修复 WireMock 测试生成，使其使用百分号编码的查询参数键，与符合规范的 HTTP 客户端发送的内容相匹配。同时将逗号分隔的查询参数示例拆分为单个 `WithParam()` 调用中的多个值，因为 WireMock.Net 会将逗号分隔的值解析为数组。
