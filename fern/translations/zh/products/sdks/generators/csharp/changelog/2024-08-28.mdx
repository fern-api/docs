## 1.5.0
**`(feat):`** 添加对服务级标头的支持。

**`(feat):`** 生成包含每个端点使用代码片段的 `snippet.json` 文件。

**`(feat):`** 应用在 `ClientOptions` 和 `RequestOptions` 类型上配置的超时。

**`(feat):`** 添加指数退避重试器，作用于 `ClientOptions` 和 `RequestOptions` 上指定的 `MaxRetries` 配置选项。

**`(feat):`** 生成包含重试逻辑测试的 `RawClientTests.cs` 文件。

**`(internal):`** 通过附加的辅助方法重构 `RawClient`，使其更易于理解。

**`(fix):`** 修复直接用作请求或响应类型的 `OneOf` 序列化失败的错误。