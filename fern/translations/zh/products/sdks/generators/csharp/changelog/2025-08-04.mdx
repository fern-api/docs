## 2.1.7
**`(fix):`** 改进生成，使所有预期的测试/样例都能正确生成和编译。


**`(fix):`** 在 `RawClient.cs` 中，命名空间引用以 `global::` 为前缀，以确保当使用显式命名空间和客户端类时可能导致冲突的情况下，类能正常工作。


**`(fix):`** 将 csharpier 版本设置为 `1.1.*`，以避免意外选择具有格式不一致性的 `1.0.3`。


## 2.1.6
**`(feat):`** 触发发布以使用新版本的 CSharpier 格式化程序