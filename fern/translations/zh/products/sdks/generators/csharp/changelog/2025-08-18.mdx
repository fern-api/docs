## 2.1.12
**`(fix):`** 添加对配置 'experimental-fully-qualified-namespaces' 的支持，以生成完全限定的类型名称和命名空间。
添加对 `experimental-dotnet-format` 的支持，使用 `dotnet format` 减少命名空间和命名空间前缀。（这可能需要大量内存）
这会创建更清洁的代码，并且可以轻松支持生成代码中具有相同名称的多个端点，而不会出现名称或命名空间冲突。