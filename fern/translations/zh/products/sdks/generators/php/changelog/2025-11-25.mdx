## 1.22.3
**`(fix):`** 修复动态代码片段以正确处理多 URL 环境中的自定义 URL。自定义 URL 现在正确生成 `Environments::custom(...)` 调用，而不是出现"Invalid multi url environment"错误。


## 1.22.2
**`(fix):`** 为生成的线路测试文件添加缺失的 `<?php` 开始标签以支持正确的语法高亮。


## 1.22.1
**`(fix):`** 通过添加 @phpstan-ignore-line 抑制来修复枚举字符串返回类型的 PHPStan 错误。


## 1.22.0
**`(chore):`** 升级到 IR 版本 62。


## 1.21.0
**`(feat):`** 为 PHP SDK 添加 OAuth 客户端凭证支持。SDK 现在生成一个 OAuthTokenProvider 类，处理带有过期处理的自动令牌刷新。根客户端接受 clientId 和 clientSecret 参数用于 OAuth 认证，动态代码片段显示正确的 OAuth 实例化示例。


## 1.20.0
**`(feat):`** 为 PHP SDK 添加 reference.md 生成。参考文档包含所有 API 端点的动态代码片段和示例数据，按服务组织。