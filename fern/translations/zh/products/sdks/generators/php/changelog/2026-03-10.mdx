## 2.1.10
**`(fix):`** 修复联合类型中布尔值的序列化和反序列化。
（匹配浮点数的现有修复。）


## 2.1.9
**`(fix):`** 在 README.md 和 reference.md 生成失败的日志记录后重新抛出错误。
文档生成错误现在会中断生成流程，因此被视为重要的待修复问题。


## 2.1.8
**`(fix):`** 修复动态代码片段以在生成的代码示例中包含全局头。
之前，通过 `x-fern-global-headers` 配置的必需全局头（例如 `X-Organization-ID`）在动态代码片段输出中缺失。还修复了头值查找以使用 wire 值而不是不存在的属性。