## 1.16.2
**`(feat):`** 在 README 中为重试和超时参数添加文档，包括如何在请求级别配置 maxRetries 和 timeout 选项的示例


## 1.16.1
**`(feat):`** 在 README 中添加异常处理文档，包含捕获和处理 API 异常的示例

## 1.16.0
**`(feat):`** 为 PHP SDK 引入 README 生成的初始版本，包含安装、使用和贡献部分。

## 1.15.1
**`(internal):`** 将 IR 更新至 v58。