## 0.14.0
**`(feat):`** 支持字面量类型

## 0.13.7
**`(fix):`** 将联合类型的构造函数设为私有并移除静态未知构造函数

## 0.13.6
**`(fix):`** 修复反序列化判别联合的值字段为 null 的问题。

## 0.13.5
**`(fix):`** 将判别联合方法名从 `->getvalue()` 更改为 `->getValue()`。

## 0.13.4
**`(fix):`** 将测试目录结构从 `tests/package-name/**` 扁平化为 `tests/**`，以匹配 composer.json 中定义的 psr-4 结构。没有此更改，您无法从测试中引用 `tests` 目录内的类，例如用于常见测试实用程序的 `Helpers` 类。

## 0.13.3
**`(fix):`** 修复空请求被 JSON 序列化为空数组而不是空对象的问题。

## 0.13.2
**`(fix):`** 修复无法在根客户端构造函数中覆盖字面量全局标头的错误。

## 0.13.1
**`(fix):`** 将联合引用呈现为其类型引用而不是 mixed。

## 0.13.0
**`(feat):`** 添加配置请求超时的支持。

**`(fix):`** 修复私有分页方法助手与魔术方法（例如 `__get`）冲突的错误。

## 0.12.0
**`(feat):`** 添加判别联合的支持。