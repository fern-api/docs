## 2.1.5
**`(chore):`** 标准化 WireMock 配置以使用 `WIREMOCK_URL` 环境变量
（例如，`http://localhost:8080`）而不是硬编码 URL。Docker Compose
现在使用动态端口映射（`0:8080`）以避免并行运行多个 SDK 测试时的端口冲突。引导脚本发现分配的
端口并自动设置 `WIREMOCK_URL`。如果未设置变量则回退到 `http://localhost:8080`。