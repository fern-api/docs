## 1.27.0
**`(feat):`** 从 Guzzle 切换到 PSR-7/17/18 HTTP 接口；SDK 现在与框架无关，应该与任何符合 PSR-18 的 HTTP 客户端（Guzzle、Symfony HttpClient 等）一起工作。SDK 使用 `php-http/discovery` 在运行时自动查找已安装的 PSR-18 客户端。重试逻辑现在通过 `RetryDecoratingClient` 装饰器实现，而不是作为 Guzzle 中间件。现有的 Guzzle 用户应该不受影响，因为 Guzzle 实现了 PSR-18。