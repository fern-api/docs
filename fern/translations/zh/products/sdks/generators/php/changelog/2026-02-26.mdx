## 2.1.4
**`(fix):`** 修复在没有默认值的属性上调用 `ReflectionProperty::getDefaultValue()` 时出现的 PHP 8.5 弃用警告。现在在 `JsonSerializableType::jsonDeserialize()` 中调用
`getDefaultValue()` 之前检查 `hasDefaultValue()`。


## 2.1.3
**`(chore):`** 直接使用 generator-cli JS API 而不是子进程生成。
从 Docker 镜像中移除 generator-cli，因为它现在通过 esbuild 捆绑。