## 2.8.0
**`(feat):`** 添加 `retryStatusCodes` 配置选项（`legacy` | `recommended`）。默认的 `legacy` 模式保留现有行为（重试 408、429 和所有 >= 500）。`recommended` 模式仅重试 408、429、502、503、504（排除 500 Internal Server Error，以避免对非幂等失败进行重试）。
