## 2.2.4
**`(fix):`** 通过动态代码片段生成器传递端点 ID，以按端点区分
生成的代码片段。


## 2.2.3
**`(fix):`** 修复具有 JSON 响应类型的端点的 204 No Content 处理。
之前，返回空主体的端点（例如 204）即使在成功响应时也会抛出
`BrevoException`。现在所有 JSON 响应返回类型都是可空的（`?Type`），
生成的代码在响应主体为空时返回 `null` 而不是抛出异常。