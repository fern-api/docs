## 5.60.0
**`(feat):`** 为 `docs.yml` 中的 `page-actions` 添加 `mcp` 选项。设置为 `false` 时，
MCP 服务器端点将被禁用，其 URL 从 `llms.txt` 和代理提示中省略，
"Connect to Claude Code"/"Connect to Cursor"按钮也会隐藏。


**`(fix):`** 为非对称 webhook 签名验证添加 `payload-format` 支持，
允许在签名载荷中包含时间戳和其他组件。此前，非对称 webhook 仅对原始正文签名，
使时间戳容差检查对重放攻击无效。
