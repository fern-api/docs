## 5.65.3
**`(fix):`** 修复翻译文档页面中相对 .md/.mdx 链接的解析问题。此前，翻译页面传递了空的 markdownFilesToPathName 映射，因此相对 markdown 链接（例如 ../reference/support-matrix.md）无法解析为其发布的 slug URL。


## 5.65.2
**`(fix):`** 改进 ledger 文档发布失败时的错误消息：显示服务端的校验问题（字段路径和消息），使得无需访问服务端日志即可看到确切的失败字段。


## 5.65.1
**`(fix):`** 改进 `fern docs dev` 在内容编辑时的重新加载性能（35 秒 → 4-5 秒）。将防抖时间从 1000 毫秒降至 300 毫秒，对仅涉及 markdown 的更改跳过后台校验，并为文档包写入生成计数器文件，以便在无需 HTTP 往返的情况下检测过期缓存条目。
