## 5.59.0
**`(feat):`** 将每种 content-type 的 OpenAPI 示例提取到标记了其内容类型的端点级 v2Examples 中。
当操作声明多种内容类型（例如 application/json 和 application/ld+json）并带有不同的示例时，
导入器现在会提升这些示例，以便文档在用户切换内容类型时显示正确的示例正文。


## 5.58.0
**`(fix):`** 修复产品组中未版本化产品的产品级着陆页被丢弃的问题，并将顶级着陆页作为回退传播到版本化导航节点。


**`(internal):`** 将 AI 示例增强路由从 FAI 恢复到 fdr-lambda-docs。
