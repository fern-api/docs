---
tags: ["seo", "configuration", "search", "bug-fix", "docs.yml"]
---

- **`(fix):`** 现在遵守在 `docs.yml` 配置中定义的 `og` 和 `twitter`
- **`(fix):`** 已恢复经过身份验证的预览
- **`(fix):`** frontmatter 标题现在优先于 MDX 文件中的 `<h1>` 标签
- **`(fix):`** 规范 URL 现在区分共享相同方法和端点名称但定义在不同 API 中的端点