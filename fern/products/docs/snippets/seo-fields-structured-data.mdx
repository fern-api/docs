<ParamField path="jsonld:breadcrumb" type="object" required={false} toc={true}>
  A complete [schema.org `BreadcrumbList`](https://schema.org/BreadcrumbList) that replaces the breadcrumb trail Fern generates from the page's position in your navigation. Emitted in the page's `application/ld+json` script alongside the `TechArticle` node.
</ParamField>
