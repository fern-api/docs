## Customize GraphQL API Reference layout

<ChangelogTags>api-reference, docs.yml</ChangelogTags>

Control how GraphQL operations appear in the sidebar using the `operation` keyword in your `docs.yml` layout. Order operations, set custom titles and slugs, hide operations, and group them into sections.

```yaml docs.yml
navigation:
  - api: GraphQL API Reference
    layout:
      - section: Plants
        contents:
          - operation: QUERY getPlant
            title: Get plant details
          - operation: MUTATION createPlant
```

<Button intent="none" outlined rightIcon="arrow-right" href="/learn/docs/api-references/generate-graphql-ref#customize-the-layout">Read the docs</Button>
