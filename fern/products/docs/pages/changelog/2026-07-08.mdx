## PostHog analytics: CI key setup

<ChangelogTags>integrations, docs.yml</ChangelogTags>

Documented how to expose the PostHog API key to your publish workflow. `${POSTHOG_API_KEY}` resolves from an environment variable at publish time, so the GitHub secret must be mapped into the workflow step's `env:` block.

<Button intent="none" outlined rightIcon="arrow-right" href="/learn/docs/integrations/analytics/posthog#set-the-api-key-in-ci">Read the docs</Button>
