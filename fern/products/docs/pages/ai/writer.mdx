---
title: Fern Writer
description: Fern Writer is a technical writing agent that automatically updates documentation when your code and products change
---

<Note>
Fern Writer is available in beta. For access, reach out via Slack or support@buildwithfern.com. 

**Coming soon**: GitHub integration for automatic updates when your repo changes.
</Note>

Fern Writer is a technical writing agent powered by [Devin AI](https://devin.ai/) that keeps your docs aligned as your product evolves. You can request documentation updates by tagging `@Fern Writer`, and it drafts targeted edits to your documentation via GitHub pull requests.

Fern Writer understands Fern components and your custom styling, learns your writing style, and makes edits that fit your existing content organization. You can customize its behavior to match your team's style guide and workflow.

## Setup

To start using Fern Writer, add it to your Slack workspace (you must be a Slack admin) and invite it to channels where your team discusses documentation.

<Steps>
<Step title="Get your unique install link">

Use the [API Explorer](/learn/docs/scribe-api/slack-scribe/get-scribe-slack-install-link) to get a unique Slack installation link for your organization. Provide:
- Your [Fern token](/learn/cli-api-reference/cli-reference/commands#fern-token)
- The GitHub repository containing your documentation in `owner/repo` format (e.g., `acme/docs`)

You can alternatively use this cURL request:
```bash
curl -G https://fai.buildwithfern.com/scribe/slack/get-install \
     -H "Authorization: Bearer <YOUR_FERN_TOKEN>" \
     --data-urlencode github_repo=<OWNER/REPO>
```

Follow the URL returned in the response.

</Step>
<Step title="Add to your workspace">
You'll be redirected to Slack to authorize Fern Writer. Select the workspace where you want to add Fern Writer and click **Allow**.

</Step>
<Step title="Add to channels">

Once installed, add Fern Writer to Slack channels where your team discusses documentation.

</Step>
</Steps>

## Usage

In channels where Fern Writer has been added, tag `@Fern Writer` and describe the change you need. Fern Writer only responds when directly tagged, and will create a pull request and reply with a link.

Example requests:
- `@Fern Writer add a section about authentication to the getting started guide`
- `@Fern Writer update the API reference to include the new /plants endpoint`
- `@Fern Writer fix the typo in the installation instructions`

Request changes by commenting in the Slack thread or directly on the PR. Fern Writer responds to feedback from both humans and automated review tools.

<Accordion title="Video example">
<video 
    src="fern-writer.mp4"
    autoPlay
    loop
    controls
    playsInline
    muted
>
</video>
</Accordion>
