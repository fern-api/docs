## 5.98.3
**`(fix):`** Fix `@/`-prefixed imports inside fenced code blocks and inline code being rewritten to relative
paths. Code samples are now rendered verbatim; only real MDX imports are resolved.


