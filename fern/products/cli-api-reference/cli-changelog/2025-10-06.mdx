## 0.84.10
**`(fix):`** Updates the `page-actions` configuration, adds new `settings` options in the `docs.yml`.

```docs.yml
settings:
  dark-code-mode: true
  http-snippets:
    - curl
    - ruby

page-actions:
  default: cursor
  options: 
    chatgpt: false
    claude: true
    vscode: true
    cursor: true
```


