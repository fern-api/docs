## 0.50.5
**`(fix):`** The Fern CLI is updated to create the organization if it doesn't exist when `fern token` is called.


## 0.50.4
**`(fix):`** The preview server is updated such that local previews of both OpenRPC and OpenAPI (legacy parser)
are now possible.


