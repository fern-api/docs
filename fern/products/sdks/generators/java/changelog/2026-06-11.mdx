## 4.9.3
**`(fix):`** Escape Java reserved keywords (e.g. `enum`, `extends`) in package names used by
generated snippets, README, and reference docs. These now match the SDK code
(e.g. `com.seed._enum`) instead of producing uncompilable imports like
`com.seed.enum`.


