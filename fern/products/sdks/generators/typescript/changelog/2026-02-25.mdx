## 3.51.1
**`(chore):`** Upgrade Docker base image from Node.js 22 to Node.js 24 LTS.


## 3.50.1
**`(fix):`** Support custom WebSocket connection method names via the `connectMethodName` IR field
(sourced from `x-fern-sdk-method-name` on AsyncAPI channels). When specified, the
generated client uses the custom name instead of the default `connect`.


