---
title: Generating an SDK
headline: Generating an SDK (Rust)
description: Get started with the Fern Rust SDK.
---


Generate a Rust SDK by following the instructions on this page. 

<llms-only>
If the user already has a `fern/` folder set up, they can skip the first two steps. Make sure they're working in their source repo (the one containing the `fern/` folder), not an SDK repo.
</llms-only>

<Prompt
  title="Generate a Rust SDK"
  actions={["cursor", "claude", "chatgpt"]}
>
Generate a Rust SDK with Fern. Follow the [Rust SDK quickstart](https://buildwithfern.com/learn/sdks/generators/rust/quickstart.md) step by step.
</Prompt>

<Steps>

<Markdown src="/products/sdks/snippets/init-fern-folder.mdx"/>

<Step title="Add the SDK generator">

Run the following command to add the Rust SDK generator to `generators.yml`:

<Template
  data={{ GROUP_NAME: "rust-sdk" }}
  tooltips={{
    GROUP_NAME: (<p>The name of the <code>generators.yml</code> group that configures your Rust SDK's output location and other metadata. You can customize this group name to differentiate between multiple SDKs across different languages.</p>)
  }}
>
```bash
fern add fern-rust-sdk --group {{GROUP_NAME}}
```
</Template>

This command adds the following `group` to `generators.yml`:

```yaml title="generators.yml"
  rust-sdk: # group name
    generators:
      - name: fern-rust-sdk
        version: <Markdown src="/snippets/version-number-rust.mdx"/>
        output:
          location: local-file-system
          path: ../sdks/rust
```
</Step>
<Step title="Generate the SDK">

Run the following command to generate your SDK:

```bash
fern generate --group rust-sdk
```

<Markdown src="/products/sdks/snippets/generate-sdk.mdx"/>

<Files>
  <Folder name="fern" comment="created by fern init" />
  <Folder name="sdks" defaultOpen comment="created by fern generate --group rust-sdk">
    <Folder name="rust" defaultOpen>
      <File name="Cargo.toml" />
      <File name="README.md" />
      <File name="reference.md" />
      <File name="rustfmt.toml" />
      <Folder name="src">
        <File name="client.rs" />
        <File name="config.rs" />
        <File name="error.rs" />
        <File name="lib.rs" />
        <File name="prelude.rs" />
        <Folder name="core">
          <File name="http_client.rs" />
          <File name="mod.rs" />
          <File name="pagination.rs" />
          <File name="query_parameter_builder.rs" />
          <File name="request_options.rs" />
          <File name="sse_stream.rs" />
          <File name="utils.rs" />
        </Folder>
        <Folder name="api">
          <File name="mod.rs" />
          <Folder name="types" />
          <Folder name="resources" />
        </Folder>
      </Folder>
    </Folder>
  </Folder>
</Files>
</Step>

<Markdown src="/products/sdks/snippets/setup-github-repos.mdx" language="rust" />

<Step title="Publish to crates.io">

Follow the [Publishing to crates.io](/learn/sdks/generators/rust/publishing) guide to configure your crate and set up automated publishing via GitHub Actions.
</Step>
</Steps>
