## 1.25.0-rc1
**`(fix):`** Fix non-deterministic ordering of generated test functions. The test generation
for JSON marshaling, String methods, enum validation, and extra properties tests
now sorts type names before iterating, ensuring consistent output across runs.


