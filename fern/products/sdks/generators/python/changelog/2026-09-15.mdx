## 5.30.2
**`(fix):`** Fix the generated OIDC publish workflow in `ci.yml` so the `pypi` GitHub Environment URL
points to the configured `package-name` (`https://pypi.org/p/<package-name>`) instead of
`github.event.repository.name`.


